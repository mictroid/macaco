# Release signing config for the upload keystore.
# Copy this file to `keystore.properties` (same dir) and fill in your real values.
# `keystore.properties` and *.jks are git-ignored — never commit them.
# See docs/release-setup.md for how to generate the keystore.

storeFile=C:/Users/micke/macaco-upload.jks
storePassword=CHANGE_ME
keyAlias=macaco-upload
keyPassword=CHANGE_ME
