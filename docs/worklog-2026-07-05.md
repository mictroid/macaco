# Macaco worklog — 2026-07-05

## landscape-layout-consistency-v2 brief (5 changes, 4 files) — IMPLEMENTED. `compileDebugKotlin` SUCCESSFUL. Brief → `docs/DONE/`. Not yet shipped.

Follow-up to the vc59 landscape batch below. All target files verified vs live source first; every
BEFORE snippet matched exactly. No new strings (nothing to localize). Not yet version-bumped/dispatched.

- **Change 1 — `MapScreen.kt`** — Adventures compact **landscape** header merged 3 lines → 2: the
  separate `macaco` wordmark `Text` folded into the title+count `Row` (icon stays on its own centred
  row above), and the header-specific `padding(horizontal = 16.dp)` dropped to `vertical = 4.dp` only,
  matching Journal's landscape recipe. Trims ~one text line + padding off the header. No new imports.
- **Change 2 — `EntryDetailScreen.kt`** — slim header brand block changed from inline `Image`+`Text` to
  a `Column` (icon **stacked above** the wordmark), matching the other screens' compact-header
  convention. Icon 32→28 dp, wordmark 16→12 sp to keep the ~48 dp header slim. `Column` already
  imported (verified L20) — no import change, brief correct.
- **Change 3 — `OnboardingScreen.kt`** — `HorizontalPager` now reserves the measured bottom-controls
  height (`onSizeChanged` → `bottomControlsHeightPx`) as bottom padding so centred intro copy can't run
  into the dots/Next button in landscape; art/text also shrink under the existing `screenHeightDp < 480`
  landscape threshold (new `compact` param on `OnboardingSlide`).
  **Deviation:** brief's import list was incomplete — it named `mutableIntStateOf`, `onSizeChanged`,
  `LocalConfiguration`, `LocalDensity` but omitted **`remember`** and **`setValue`**, both required for
  `var bottomControlsHeightPx by remember { mutableIntStateOf(0) }` (file previously imported only
  `getValue`/`rememberCoroutineScope`). Added all six.
- **Change 4 — `JournalListScreen.kt`** — content `Column` inset swapped from
  `navigationBars.only(End)` to `safeDrawing.only(Horizontal)` so the front-camera-cutout side is inset
  too, matching Help & About's default Scaffold insets (fixes entries rendering behind the cutout).
  `safeDrawing`/`WindowInsetsSides`/`windowInsetsPadding` already imported — no import change, brief correct.
- **Change 5 (5a/5b) — `JournalListScreen.kt`** — profile-avatar edge padding swapped from hardcoded
  4 dp (landscape) / 12 dp+row-4 dp (portrait) to `macacoContentGutter()`, so the avatar lines up with
  the tag row / entry list gutter below at every width. Avatar's own `end = 12.dp` dropped in 5b since
  the row gutter now supplies the margin. `macacoContentGutter` already imported (L93) — brief correct.

**Verification note:** only Change 3's import claim was wrong (2 missing); the other three "already
imported" claims all checked out against live source.

**Open:** on-device verify (S8+ debug, premium login) before shipping; not yet version-bumped/dispatched.

## vc59 / 1.6 SHIPPED — landscape/tablet layout batch — WIF run `28739282883` ✅ CONFIRMED SUCCESS

Dispatched **2026-07-05 13:30:08 +02:00** (11:30:09 UTC) →
https://github.com/mictroid/macaco/actions/runs/28739282883. Bump+notes commit `93d5817`
(vc58→59, versionName 1.6); **origin == HEAD (`93d5817`) verified before dispatch**.
**✅ CONFIRMED SUCCESS (6m14s) — PUBLISHED to closed testing** (verified via `gh run view` 2026-07-05). Batch:
- The **landscape-layout-consistency brief** (7 changes, commit `5b10e65`): shared 840dp content-width
  gutter + `macacoContentGutter()`, horizontal safe-drawing insets (A53 side-nav fix on Profile),
  slim branded / collapsing headers on Journal + Help & About + Entry Detail.
- **Landscape header refinements** (commits `6fa8d2f`, + the icon-size fix): unified landscape brand
  headers (icon on its own centred row on Journal/Help/Adventures), collapsed→centred icon at full
  uncollapsed size, Help expanded icon separation, Adventures header safe-drawing inset + icon sizing,
  Profile back-arrow removal, Journal entry padding aligned with Help & About.

On-device verify done on the A53 for the brief + most refinements (both orientations); the final
landscape at-rest icon-size bump (Journal 48 / Help 64 / Adventures 48 dp) shipped **without** on-device
verification at the user's request ("just adjust and ship, no verification").

## Landscape header refinements (post-brief, on-device A53 review) — SHIPPED with the brief.

After verifying the landscape-layout brief on the A53 (3-button nav, premium account), the user asked
for a round of header refinements across the brand headers. Iterated on-device with screenshots:

- **Unified landscape brand headers (Journal, Help & About, Adventures/MapScreen).** In landscape the
  icon now sits on **its own centred row** (not inline with the wordmark), with the wordmark + subtitle
  beneath — the three screens read identically. Journal keeps the profile avatar anchored top-end; Help
  keeps the back arrow top-start; Adventures has neither.
- **Collapsed (scrolled) header → centred icon only.** Journal and Help both reduce on scroll to a slim
  bar with just the macaco icon centred in the brand fade (no wordmark/count/avatar). The icon keeps its
  **full uncollapsed size** (Journal 48dp, Help 64dp) with a bottom padding nudging it up toward the
  fade's centre (per user preference — a smaller 28dp version was rejected as "reduced").
- **Help & About expanded header:** removed the wordmark pull-up so the 64dp icon stands clearly alone
  on its own row above the wordmark.
- **Adventures/MapScreen header** now also applies the horizontal `safeDrawing` inset so the centred
  brand block stays centred with the page content past a side nav bar; portrait icon 44→48dp (Journal
  parity), landscape icon 32dp on its own row.
- **ProfileScreen:** removed the banner back arrow (Profile is a bottom-nav tab, so it was redundant) —
  dropped the `onBack` param + its NavGraph arg + the unused `ArrowBack` import.
- **Journal entry padding:** entry-card content padding 12→16dp and month-header `start=4dp` so the list
  content lines up on the left with Help & About's cards. (Entry photos stay full-bleed by design.)

Files: `JournalListScreen.kt`, `HelpAboutScreen.kt`, `ProfileScreen.kt`, `MapScreen.kt`, `NavGraph.kt`.
`assembleDebug` SUCCESSFUL; verified on A53 in both orientations. Removed now-unused imports
(`AnimatedVisibility`/`expandVertically`/`shrinkVertically` in both list screens; `clip`/`TopAppBar*` in
Help; `offset` in Help).

## landscape-layout-consistency brief (7 changes, 4 files + 1 new) — IMPLEMENTED. `assembleDebug` SUCCESSFUL. Brief → `docs/DONE/`. Not yet shipped.

Single large Cowork brief making the journal list, Help & About, Profile, and Entry Detail behave
consistently in landscape and on tablets. All target files verified vs live source first; every BEFORE
snippet matched. `assembleDebug` green (1m03s).

- **Change 1 — `ui/theme/ContentWidth.kt` (new)** — shared 840 dp max-content-width token
  (`MacacoContentMaxWidth`) + `macacoContentGutter(min=16.dp)` that centres content once the screen is
  wider than the cap. File already existed untracked from an earlier session; content matched the brief
  verbatim, so it was left as-is and picked up by this commit.
- **Change 2 — `JournalListScreen.kt`** — list gutter replaced the ad-hoc `if (screenWidthDp>=600) 80.dp
  else 16.dp` with `macacoContentGutter()`. Added `import …ui.theme.macacoContentGutter`.
- **Change 5 — `JournalListScreen.kt`** — landscape header now scroll-hides: outer branded Box keeps the
  status-bar brand strip + gains `windowInsetsPadding(safeDrawing Horizontal)`; the existing compact/tall
  `if/else` moved inside a new `AnimatedVisibility(visible = !(isLandscape && collapsed))` → inner
  animateContentSize Box (two extra closing braces). Added imports `AnimatedVisibility`,
  `expandVertically`, `shrinkVertically`, `safeDrawing`.
- **Change 3 — `HelpAboutScreen.kt`** — content column scroll state hoisted (`scrollState`) and gutter
  switched to `padding(horizontal = macacoContentGutter(), vertical = 16.dp)`.
- **Change 6 — `HelpAboutScreen.kt`** — `TopAppBar` + in-content brand band replaced by a journal-style
  collapsing branded header (tall brand block at rest, slim centred row when scrolled/landscape,
  scroll-hides in landscape). Added the animation/inset/`derivedStateOf`/`LocalConfiguration` imports;
  **removed now-unused `TopAppBar`, `TopAppBarDefaults`, and `draw.clip` imports** (clip was only on the
  deleted band).
- **Change 4 — `ProfileScreen.kt`** — horizontal `safeDrawing` insets re-applied to the four content
  siblings below the edge-to-edge banner (identity column, divider, action-tile grid, Delete Account
  button) to fix the A53 3-button side-nav overlap that the single-column rework had dropped. Added
  `WindowInsetsSides`/`only`/`safeDrawing`/`windowInsetsPadding` imports (`WindowInsets` already present).
  **Deviation per the brief's own note:** also applied the same inset to the "Member since" footer, the
  one further sibling below Delete Account.
- **Change 7 — `EntryDetailScreen.kt`** — full-height themed `TopAppBar` replaced by a slim ~48 dp
  `macacoBrandBackground()` header row (back + counter lead, brand block centred in leftover space via
  weighted spacers, wordmark drops below 420 dp wide, share/edit/delete trail). A brand moment under the
  chrome policy, so the fixed splash palette is intended. Added `Image`, `painterResource`, `offset`, and
  the `WindowInsets` companion imports; **removed now-unused `TopAppBar`/`TopAppBarDefaults` imports**.

**Verification notes:** `macacoBrandBackground`, `SplashGold`, `SplashGoldBright`, `SplashTealMid` are
defined in `SplashScreen.kt` (same `ui.screens` package) — no import needed, matching the brief. Only
`macacoContentGutter` (in `ui.theme`) required a new import in the two list screens. No BEFORE mismatches.

**Out of scope (per brief):** SettingsScreen (already has `.navigationBarsPadding()`), the full-bleed tag
filter row, and no new strings (reuses existing keys).

**Open:** on-device verify (S8+ debug, premium login) before shipping; not yet version-bumped/dispatched.
