# Worklog — 2026-07-11

## Implemented (not yet shipped) — feature batch 1 (weather-stamp + entry-search)

7-brief Cowork drop being worked in batches of ~2 with a `/clear` between (cost discipline — see the
`subagent-model-discipline` memory; build subagent now pinned to Haiku, localize to Sonnet in the
implement-cowork-brief skill). Batch 1, both briefs verified vs live source first, `assembleDebug`
passed. Both → `docs/DONE/`. Awaiting the rest of the batches + one S8 verify pass before shipping.

- **weather-stamp** — historical weather chip on entries via Open-Meteo's free archive API (no key).
  - `data/model/TravelEntry.kt`: added `weatherCode: Int?` / `weatherTempMaxC: Double?` (both null-
    default, back-compatible).
  - `util/WeatherLookup.kt` (NEW): geocode (deprecated `Geocoder`, matching the app's existing
    pattern) + Open-Meteo archive call over plain `HttpURLConnection`/`org.json`; WMO-code→emoji/label
    `describe()` and locale-aware `formatTemp()` (°F for US locale, else °C).
  - `data/storage/CloudEntrySync.kt`: Firestore read (`getLong("weatherCode")?.toInt()`,
    `getDouble("weatherTempMaxC")`) + `toMap()` write of both fields.
  - `ui/viewmodel/JournalViewModel.kt`: lazy fetch-once-per-entry background `launch` inside
    `saveEntry` (sibling to the Drive-upload launch), guarded on `weatherCode == null` + non-blank
    location + non-future date; added `import …util.WeatherLookup`.
  - `ui/screens/EntryDetailScreen.kt`: weather `AssistChip` (secondaryContainer tokens, matching the
    mood chip) added after the location chip in BOTH portrait + landscape layouts; added
    `import …util.WeatherLookup`.
  - Deviation vs brief: comment in TravelEntry referenced a non-existent `maybeFetchWeather` helper;
    corrected to point at `saveEntry` (where the fetch actually lives).
  - Strings: 10 keys × 11 locales (`weather_*`); `weather_temp_c/f` + `weather_unknown` kept literal.

- **entry-search** — full-screen client-side search over title/description/location/tags/trip name.
  - `data/model/TravelEntry.kt`: `List<TravelEntry>.matchingSearch(query)` extension (added after
    `tagsByFrequency`; brief's cited neighbors `tripNames()`/`locations()` don't exist in this file —
    they're `toTripSuggestions()`/`toLocationSuggestions()` in NavGraph, harmless placement note).
  - `ui/screens/JournalListScreen.kt`: `onSearch: () -> Unit` param; search `IconButton` in the
    portrait header (replaced the leading 40dp spacer, brand block stays centred) + a TopStart search
    box in the landscape header; dropped `private` on `EntryCard` so search reuses it; added
    `import …material.icons.filled.Search`.
  - `ui/screens/SearchScreen.kt` (NEW): auto-focused `TextField`, three states (hint / no-results /
    results reusing `EntryCard` with `selectedTags = emptySet()`).
  - `ui/navigation/Screen.kt`: `object Search`. `ui/navigation/NavGraph.kt`: `composable(Search)` +
    `onSearch` wiring + `SearchScreen` import.
  - Deviation vs brief: brief's SearchScreen import block used `Color.Transparent` but omitted
    `import androidx.compose.ui.graphics.Color` — added it.
  - Strings: 4 keys × 11 locales (`search_*`); `common_back` already existed.

## Implemented (not yet shipped) — feature batch 2 (camera-roll-suggestions + shared-trip-links)

7-brief Cowork drop, batch 2 (of the batches-of-~2 plan). Both briefs verified vs live source
first; `assembleDebug` passed (see per-brief deviations). Both → `docs/DONE/`. NOT yet shipped —
shared-trip-links in particular is **not shippable/testable** until manual out-of-repo steps (below).

- **camera-roll-suggestions** — scan recent geotagged camera-roll photos, cluster by time+location,
  surface a dismissible "New photos from <place>" banner that pre-fills a new entry.
  - `AndroidManifest.xml`: added `ACCESS_MEDIA_LOCATION` (load-bearing for unredacted GPS EXIF).
  - `util/PhotoRollScanner.kt` (NEW): MediaStore query (last 14 days) + `setRequireOriginal` EXIF GPS
    read + time/location clustering + reverse-geocode + coarse de-dup vs existing entries. Dropped the
    brief's unused `import java.util.Calendar`.
  - `data/PreferencesManager.kt`: `dismissedPhotoClusters` set + `dismissPhotoCluster()`. Deviation:
    brief used a bare `dataStore`/`stringSetPreferencesKey`; this file addresses DataStore as
    `context.dataStore` and did NOT import `stringSetPreferencesKey` — added the import, matched the
    `context.dataStore` + `.catch { emit(emptyPreferences()) }` convention.
  - `ui/viewmodel/JournalViewModel.kt`: `suggestedClusters` + `pendingEntrySeed` state,
    `checkForSuggestedEntries` (once-per-process guard) / `dismiss` / `accept` / `entrySeedConsumed`;
    `EntrySeed` data class; added `import …util.PhotoRollScanner`.
  - `ui/screens/JournalListScreen.kt`: permission-invite card (soft opt-in, no cold OS dialog) +
    one suggestion card per cluster as LazyColumn items after the on-this-day banner;
    `RequestMultiplePermissions` launcher (media-read API-split + `ACCESS_MEDIA_LOCATION`), calls
    `suppressAutoLockOnce()` before requesting so the app-lock timer isn't tripped. New
    `SuggestionCard`/`SuggestionPermissionCard` composables. Invite dismissal is `rememberSaveable`
    (per-process, not a persisted pref — minor deviation from "one-time", keeps scope tight).
  - `ui/screens/NewEditEntryScreen.kt` + `NavGraph.kt`: **Deviation (brief Change 6 didn't compile
    as written)** — this screen takes NO `viewModel` param (fully callback-based), so the brief's
    `viewModel.pendingEntrySeed`/`entrySeedConsumed()` calls were impossible. Added a
    `seed: JournalViewModel.EntrySeed?` param + `onSeedConsumed` callback, wired from the ViewModel in
    NavGraph's `NewEntry` route. **Also**: brief claimed seeded photos get persisted "at save time via
    persistToGallery" — that's WRONG (persist happens at *pick* time). Fixed: the seed-consumption
    `LaunchedEffect` persists the original camera-roll URIs into Pictures/Macaco (on `Dispatchers.IO`,
    reusing `ImageStorage.persistToGallery`), marks them `sessionAdded`, pads `driveIds`.
  - Strings: 7 keys × 11 locales (`photo_suggestion_*`).

- **shared-trip-links** — generate a read-only public web link for a trip (Android side only).
  ⚠️ **NOT shippable/testable yet** — needs, outside this repo: (1) `/shared_trips` Firestore +
  Storage security rules pasted into the Firebase console (until then every `createShareLink` fails
  permission-denied); (2) the `mictroid.github.io/macaco/trip/` viewer page (separate static-web
  task); (3) a **privacy-policy update** disclosing the new public data flow before real users.
  - `app/build.gradle.kts`: added `implementation(libs.firebase.storage)`. Deviation: brief used a raw
    `implementation("com.google.firebase:firebase-storage-ktx")` string; the `firebase-storage` alias
    **already existed** in `gradle/libs.versions.toml` (unused) — used the catalog alias to match the
    file's convention.
  - `data/sync/TripShareManager.kt` (NEW): create (capped 12-photo Storage upload + redacted Firestore
    doc) / revoke / listMyShares. Deviation: brief's `revokeShareLink` returned `Void` from the final
    `delete().await()`, mismatching `Result<Unit>` — added an explicit `Unit`.
  - `ui/viewmodel/JournalViewModel.kt`: `TripShareState` sealed class + `createTripShare` /
    `revokeTripShare` / `tripShareConsumed`; `tripShareManager`; added `import …data.sync.TripShareManager`.
  - `ui/screens/JournalListScreen.kt`: `TripHeader` gets an `onShare` param + a `Share` IconButton
    (next to the premium reel button, same `isPurchased` gate); new `ShareTripDialog` (mandatory
    expiry radio 7/30/never defaulting to 30, required disclosure text, Creating spinner, Ready
    url+copy(Clipboard)+ACTION_SEND share, Error retry) + `ExpiryOption` helper.
  - Scope note: `listMyShares` + revoke exist in the manager/VM but the v1 dialog is create-only (no
    "my shares" list UI) — `trip_share_revoke`/`_revoked` strings added for future use per the brief's
    string table but not yet surfaced.
  - Strings: 13 keys × 11 locales (`trip_share_*`).

## Implemented (not yet shipped) — reel-outro-hold-and-brand-icon-size

- **reel-outro-hold-and-brand-icon-size** — 3-change brief, all verified vs live source first,
  `compileDebugKotlin` passed, no deviations. Brief → `docs/DONE/`. Awaiting next version bump.
  1. **`data/sync/AdventureReelEncoder.kt`**: `OUTRO_HOLD_FRAMES` 45 → 120 (1.5 s → 4 s) so the QR
     end-card holds long enough to scan before the clip loops. Constant is read in the hold loop
     (~line 283) and the total-length calc (~line 116); both pick up the new value automatically
     (total reel length grows ~2.5 s, as intended).
  2. **`ui/components/MacacoBrandBlock.kt`**: landscape `collapsedIconSize` 28.dp → 36.dp (shared by
     Journal / Map / Profile compact headers).
  3. **`ui/screens/EntryDetailScreen.kt`**: hand-rolled header icon 28.dp → 36.dp (line ~343) to
     match #2 — this is the one header not routed through `MacacoBrandBlock`. (Deduping it into the
     shared component was explicitly flagged out-of-scope / future brief.)

## vc66 (1.6, closed testing) — DISPATCHED 2026-07-11

- **vc66 / 1.6 — SHIPPED ✅ CONFIRMED SUCCESS 2026-07-11** (WIF run `29144641904`, ~6m24s
  07:31:48→07:38:12 UTC, PUBLISHED to closed testing; dispatched 09:31:46 +0200, impl commit `40af595`,
  bump commit `5b6e29e`; origin == HEAD `5b6e29e` verified before dispatch). Shipped without on-device
  verify. 4-brief batch, all verified vs live source first, `assembleDebug` passed, no deviations.
  All 4 briefs → `docs/DONE/`.

### Batch contents (4 briefs)

1. **adventure-reel-outro-card-v3** (`util/AppActions.kt`): `REEL_SHARE_URL` corrected from the full
   percent-encoded tracked Play URL to the short redirect `https://mictroid.github.io/macaco/r/` — the
   full URL wrapped into a six-line `%3D`/`%26` wall in share captions. Re-applies the correction that
   was lost in vc65's Fix 3 (shipped an earlier draft). Value + comment only; QR unaffected.

2. **drive-video-download-fix** (`data/sync/DrivePhotoSync.kt`): deleted the premature
   `if (needed.isEmpty()) return@withContext` in `downloadMissing()` (was ~line 250) that skipped the
   video-download block for video-only entries → data-loss fix (clips now pull from Drive on other
   devices / after reinstall, and are included in backup export). The combined
   `if (needed.isEmpty() && neededVideos.isEmpty())` guard below is now the sole gate.

3. **paywall-locale-translations** (`res/values-*/strings.xml`, 10 locales): added all missing keys —
   `de` 18, `es/fr/it/ja/nl/pl/pt/zh-rCN` 20 each, `sv` 47 (20 common + 27 backlog). Closes QA S1+S2;
   the multi-plan paywall (`purchase_*`) was rendering half-English in translated markets. Completeness
   check: all 10 locales report 0 missing keys vs base. Done via Edit tool (no BOM added — CJK/accents
   intact).

4. **release-hygiene** (`ui/screens/MapScreen.kt`, `MainActivity.kt`, `data/sync/JournalBackup.kt`,
   `res/xml/{data_extraction_rules,backup_rules}.xml`): removed 3 `Log.d("MapCamera",…)` diagnostics
   (kept the `Log.e` failure signal, `Log` import still used); excluded `datastore/wanderlog_prefs`
   from cloud backup on both API paths (device-transfer left open); fixed stale comments
   (`Pictures/Wanderlog`→`Macaco`; nonexistent `downloadMissingVideos`→`DrivePhotoSync.downloadMissing()`
   ×2 in JournalBackup — this comment fix subsumes brief 2's optional Change 2). Closes QA L2/L5/L6.
   Note: brief 2 and brief 4 both named the JournalBackup comment fix; applied once here.

Also committed the two Cowork QA reports the briefs reference (`docs/qa-report-2026-07-11.md`,
`docs/production-readiness-2026-07-11.md`).

### Open items

- On-device verification of vc66 (user logs in with a premium account to clear the paywall):
  video-only entry download on a second device / after reinstall; paywall shows fully translated in a
  non-English locale; reel share caption shows the short link on one line.
- vc66 run not yet confirmed — check with `gh run list --workflow=release.yml`.
