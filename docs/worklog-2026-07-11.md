# Worklog — 2026-07-11

## vc66 (1.6, closed testing) — DISPATCHED 2026-07-11

- **vc66 / 1.6 — DISPATCHED 2026-07-11** (WIF run `29144641904`, dispatched 09:31:46 +0200,
  impl commit `40af595`, bump commit `5b6e29e`; origin == HEAD `5b6e29e` verified before dispatch).
  NOT yet confirmed; shipped without on-device verify. 4-brief batch, all verified vs live source
  first, `assembleDebug` passed, no deviations. All 4 briefs → `docs/DONE/`.

### Batch contents (4 briefs)

1. **adventure-reel-outro-card-v3** (`util/AppActions.kt`): `REEL_SHARE_URL` corrected from the full
   percent-encoded tracked Play URL to the short redirect `https://mictroid.github.io/macaco/r/` — the
   full URL wrapped into a six-line `%3D`/`%26` wall in share captions. Re-applies the correction that
   was lost in vc65's Fix 3 (shipped an earlier draft). Value + comment only; QR unaffected.

2. **drive-video-download-fix** (`data/sync/DrivePhotoSync.kt`): deleted the premature
   `if (needed.isEmpty()) return@withContext` in `downloadMissing()` (was ~line 250) that skipped the
   video-download block for video-only entries → data-loss fix (clips now pull from Drive on other
   devices / after reinstall, and are included in backup export). The combined
   `if (needed.isEmpty() && neededVideos.isEmpty())` guard below is now the sole gate.

3. **paywall-locale-translations** (`res/values-*/strings.xml`, 10 locales): added all missing keys —
   `de` 18, `es/fr/it/ja/nl/pl/pt/zh-rCN` 20 each, `sv` 47 (20 common + 27 backlog). Closes QA S1+S2;
   the multi-plan paywall (`purchase_*`) was rendering half-English in translated markets. Completeness
   check: all 10 locales report 0 missing keys vs base. Done via Edit tool (no BOM added — CJK/accents
   intact).

4. **release-hygiene** (`ui/screens/MapScreen.kt`, `MainActivity.kt`, `data/sync/JournalBackup.kt`,
   `res/xml/{data_extraction_rules,backup_rules}.xml`): removed 3 `Log.d("MapCamera",…)` diagnostics
   (kept the `Log.e` failure signal, `Log` import still used); excluded `datastore/wanderlog_prefs`
   from cloud backup on both API paths (device-transfer left open); fixed stale comments
   (`Pictures/Wanderlog`→`Macaco`; nonexistent `downloadMissingVideos`→`DrivePhotoSync.downloadMissing()`
   ×2 in JournalBackup — this comment fix subsumes brief 2's optional Change 2). Closes QA L2/L5/L6.
   Note: brief 2 and brief 4 both named the JournalBackup comment fix; applied once here.

Also committed the two Cowork QA reports the briefs reference (`docs/qa-report-2026-07-11.md`,
`docs/production-readiness-2026-07-11.md`).

### Open items

- On-device verification of vc66 (user logs in with a premium account to clear the paywall):
  video-only entry download on a second device / after reinstall; paywall shows fully translated in a
  non-English locale; reel share caption shows the short link on one line.
- vc66 run not yet confirmed — check with `gh run list --workflow=release.yml`.
