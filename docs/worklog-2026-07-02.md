# Macaco worklog — 2026-07-02

## vc49 — 6-brief vc48-QA batch (commit `7b83b4c`) — implemented, one push

Six Cowork briefs from vc48 on-device QA. All verified vs live source first; `assembleDebug`
SUCCESSFUL; all → `docs/DONE/`. User confirmed all 6 in one push (vc49). Dispatch details appended
below once shipped.

- **rotation-crash** (`AndroidManifest.xml`) — `MainActivity` `configChanges` extended to
  `locale|layoutDirection|orientation|screenSize|screenLayout` so the Activity is no longer recreated
  on rotation (Compose recomposes off `LocalConfiguration`). Fixes the rotate-to-landscape crash. No
  Kotlin changes.
- **map-camera-v7** (`MapScreen.kt`) — map Box `fillMaxSize()` → `weight(1f).fillMaxWidth()` so
  `mapSizePx` measures the remaining (visible) map height, not the full Column; northern pins stop
  rendering behind the tall tablet/portrait header, and arrow y-offsets get the right height.
- **map-nav-bar** (`MapScreen.kt`) — east chevron gets
  `windowInsetsPadding(WindowInsets.navigationBars.only(WindowInsetsSides.End))` before its 8dp end
  padding so the landscape nav bar can't intercept its taps. Added the 5 WindowInsets imports (none
  were present). West chevron untouched.
- **map-pin-theme** (`MapScreen.kt`) — `createTealMarkerBitmap`→`createThemedMarkerBitmap(context,
  colorInt)`; pin colour = `colorScheme.primary.toArgb()`; `LaunchedEffect(primaryColorArgb, mapLoaded)`
  refreshes the bitmap on theme switch; `tealMarker`→`themedMarker`. Added `toArgb` import.
- **drawer-landscape** (`JournalListScreen.kt`) — landscape drawer swaps the `weight(1f)` spacer for a
  fixed 8dp spacer and halves both `HorizontalDivider` vertical paddings (8→4dp) so Sign Out isn't
  pushed off the short screen. Portrait unchanged.
- **profile-landscape-v3** (`ProfileScreen.kt`, supersedes v2) — Fix0: `isLandscape` no longer 2-panes
  tablet PORTRAIT (`screenWidthDp>=600 && screenWidthDp>screenHeightDp`). Fix1: branded header moved
  out of the left pane into a wrapping full-width Column (back arrow + gold-tinted icon + macaco +
  avatar thumbnail). Fix2: right pane gets a Trips stat + side-by-side Subscribe/Sign Out (weight 1f,
  ellipsis) + Delete below; `tripCount` hoisted above the branch, left-pane duplicate removed. Fix3:
  stats card horizontal padding 16→8dp.
  - **Deviations:** added `ColorFilter`, `Icons.Outlined.Flight`, `TextOverflow` imports (all absent).
    Used the live-verified string keys `common_subscription`/`common_sign_out` and the `onSubscription`
    callback — the brief cited `profile_subscribe`/`profile_sign_out`/`onSubscribe`, which don't exist.
    Kept the `else` Sign-In branch + the bottom macaco logo (v3's right-pane snippet dropped them; kept
    for no-regression). `Icons.Outlined.WorkspacePremium` now unused in landscape (still used in
    portrait).

### On-device verify owed (once vc49 publishes)
- **rotation-crash** (highest priority): rotate every screen portrait↔landscape — no crash.
- **map-camera-v7**: tablet portrait / any tall header — northern pins not hidden behind the header.
- **map-nav-bar**: A53 landscape — east chevron is tappable (not under the nav bar).
- **map-pin-theme**: switch theme with the map open — pins recolour to the theme primary.
- **drawer-landscape**: A53 landscape drawer — Sign Out visible without scrolling.
- **profile-landscape-v3**: phone landscape (full-width header, Trips + side-by-side buttons, stats fit)
  + tablet portrait (single column, NOT 2-pane) + tablet landscape (2-pane).

## vc48 dispatched — 7-brief vc47-QA batch

Shipped to Play closed testing via the WIF workflow.

- **versionCode:** 47 → 48 (versionName 1.6 unchanged)
- **HEAD at dispatch:** `18ab031` — origin == HEAD verified before dispatch
- **Run ID:** 28597962578 — https://github.com/mictroid/macaco/actions/runs/28597962578
- **Dispatched:** 2026-07-02 16:30:29 +0200 (local dispatch time; `gh run list` shows run start)
- **Status:** dispatched, not yet confirmed (~6 min; check `gh run list`).
- **On-device before ship:** map-arrows-v14 verified on the A53 (debug sideload) — Argentina ⇄ Europe
  ⇄ Japan nearest-pin cycle, arrows hide at the extremes, dynamic vertical offset, theme-token teal.
  A53 now holds the debug build (Play build uninstalled to sideload) — reinstall from Play once vc48
  publishes. Other 6 briefs on-device-unverified (user chose map-arrows-only).

### Release notes (refreshed for vc48)
- Adventures map ◀▶ arrows jump to nearest off-screen trip + follow its position
- Smoother Adventure Reels (no pan/zoom jump between photos)
- Landscape polish (journal/settings/profile) + roomier tablet layouts
- Fixed black flash on language switch in dark mode
- Branded date picker + bug fixes

## vc47-QA batch implemented (7 briefs, one push) — NOT yet shipped

Cowork drafted 7 briefs from the on-device QA of vc47. All verified against live source first,
implemented, and built together (`assembleDebug` SUCCESSFUL). Batched for a single version bump.
All 7 moved to `docs/DONE/`.

Scope decision (confirmed with user): all 7 in one push; map-arrows chevrons use theme tokens
(`primaryContainer`/`onPrimaryContainer`), not the v13 hardcoded teal+gold.

### Briefs

- **map-arrows-v14** (`MapScreen.kt`) — reactive off-screen pin tracker (new `LaunchedEffect` keyed
  on `cameraPositionState.position`) replacing the one-time computation, fixing stale-coordinate
  longitude wraparound; nearest-pin navigation; `arrowVerticalFraction()` helper for dynamic vertical
  arrow placement toward the pin's latitude; navigate to the pin's actual lat; arrows decoupled from
  `globeSpanning` (now show whenever off-screen pins exist → tablet landscape). Chevrons use theme
  tokens.
  - **Deviations:** brief claimed `density` was "already in scope" — it was a local `val` inside the
    first `LaunchedEffect` only; hoisted `density` to composable scope (removed the inner decl).
    Added imports `IntOffset`, `kotlin.math.roundToInt`, `kotlin.math.PI` (all absent; brief's helper
    used bare `PI`). Now-dead `mapLatCenter` reads left in place (harmless; still written).

- **ui-polish** (`NewEditEntryScreen.kt`, `MapScreen.kt`) — date picker branded via
  `DatePickerDefaults.colors()` (surface container + primary accents); landscape map header icon
  `size(24dp)` → `size(20dp).offset(y=-2dp)`.
  - **Deviation:** added `import androidx.compose.material3.DatePickerDefaults` (absent).

- **adventure-reel-v3** (`AdventureReelEncoder.kt`) — Ken Burns continuity across the dissolve
  boundary (cap `t` at 1.0; main loop starts at `FADE_FRAMES` for photos after the first); four
  pre-allocated class-level Paints replacing per-frame allocation (A53 GC-stall fix).
  - **Deviation:** the brief's literal main-loop snippet started ALL photos at `FADE_FRAMES`, which
    would skip the first photo's opening pan. Used `mainStart = if (prev != null) FADE_FRAMES else 0`
    to match the brief's own prose (first photo has no dissolve). Frame counts unchanged.

- **journal-landscape** (`JournalListScreen.kt`) — landscape header brand content centred between
  fixed 40dp flanking elements; landscape-aware compact drawer header (slim Row) so bottom nav
  (sign-out) stays on-screen. Portrait unchanged.

- **profile-landscape-v2** (`ProfileScreen.kt`) — 2-pane now activates on tablets
  (`|| screenWidthDp >= 600`); left-pane top spacer 16dp → 8dp.

- **settings-landscape** (`SettingsScreen.kt`) — compact landscape header (slim Row); Theme Color +
  Background Image side-by-side in landscape (weight(1f) each); picker button 80dp → 44dp inline row
  in landscape (no-image state).
  - **Deviation:** brief claimed `LocalConfiguration` was already imported — it was NOT (build failed
    on it); added `import androidx.compose.ui.platform.LocalConfiguration`. Extracted the two cards to
    `@Composable () -> Unit` lambda vals (rather than duplicating the large card bodies) so both
    orientations share one definition; hint text hidden in landscape to save vertical space.

- **language-switch-flash** (`res/values/themes.xml`, `MainActivity.kt`) — added
  `android:windowBackground=@color/splash_background` to `Theme.Macaco.Splash` (was inheriting black);
  guarded `installSplashScreen()` with `if (savedInstanceState == null)` so recreations (locale/
  rotation) skip the theme-swap frame. Kills the first-language-switch black flash in dark mode.

### On-device verify owed after publish
- **map-arrows-v14** (highest risk): Europe/Japan/USA pin cycle — left arrow from Japan → Europe (not
  USA), right disappears; cycle indefinitely without sticking on USA; arrows float toward pin latitude;
  tablet-landscape arrows appear even without zoom clamp. (`adb logcat -s MapCamera`.)
- **adventure-reel-v3**: scrub a 3+ photo reel frame-by-frame at each transition — no backward pan jump;
  dissolve still silky; single-photo reel still reaches progress 1.0.
- landscape layouts on A53 (journal header centred + drawer sign-out visible; settings side-by-side +
  compact header; profile spacer) and tablet (profile 2-pane; settings stays portrait single-column).
- language-switch-flash: dark mode → Settings → Language change (no black flash); rotation (no flash);
  cold start still shows teal+monkey splash.
