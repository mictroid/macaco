# Macaco worklog — 2026-07-10

## vc65 / 1.6 SHIPPED — 3-brief follow-up batch (reel outro v2, header alignment, map-tab flash) — WIF run `29110384308` ✅ CONFIRMED SUCCESS

**✅ CONFIRMED SUCCESS** (~6m13s, 17:16:21→17:22:34 UTC), PUBLISHED to closed testing. Dispatched
**2026-07-10 19:16:20 +02:00** (17:16:21 UTC) → run
[`29110384308`](https://github.com/mictroid/macaco/actions/runs/29110384308). versionCode 64→65,
versionName 1.6. Impl+push commit `710ce3c`; bump+notes commit `d56ec66`; **origin == HEAD
(`d56ec66`) verified before dispatch**. `assembleDebug` BUILD SUCCESSFUL. All 3 → `docs/DONE/`.

**On-device verification: user WAIVED the pre-ship A53 check** — the A53 had the Play-signed build
installed (debug sideload needs an uninstall + premium re-login, which the user declined). Portrait
Profile no-scroll is code-reasoned safe (vc56 confirmed portrait fit; the topBar hoist adds no
content and preserves the height budget; `verticalScroll` intact so worst case scrolls, never clips)
but NOT live-confirmed on this build. Verify post-publish if a debug build lands on a device: Profile
portrait doesn't scroll + pinned-header avatar overlap at the expand↔collapse transition; reel outro
QR clears the player pause icon + pill glyph vs long location strings.

Three vc64 follow-up Cowork briefs, all signatures verified vs live source first. `assembleDebug`
BUILD SUCCESSFUL. All 3 → `docs/DONE/`.

**code-brief-map-tab-transition-flash.md** (`MapScreen.kt`, Changes 1 & 2 only):
1. Loading scrim `background(colorScheme.background)` → `macacoBrandBackground()` and spinner
   `colorScheme.primary` → `SplashGoldBright` — kills the ~1s stark-white blank-panel "flash" on
   Journal→Adventures navigation (was a hard color cut below the teal header).
2. Off-screen-pin chevron tracker `LaunchedEffect` gains `cameraPositioned` in its key list + guard,
   so the chevrons no longer pop in against the default framing during the loading scrim.
   - **Change 3 (A53 timestamped-logging diagnostic) deliberately NOT implemented** — it's a
     measurement task needing the device in hand; Cowork re-scoped the brief to the two code fixes.

**code-brief-header-alignment-fixes.md** (2 files):
1. `MapScreen.kt` — the header's horizontal safe-drawing inset is now scoped to the EXPANDED state
   only (`.then(if (hasMovedMap) Modifier else Modifier.windowInsetsPadding(...))`), so the collapsed
   bare icon centres on the true screen width matching the edge-to-edge map (was drifting left).
2. `ProfileScreen.kt` — banner `Box` hoisted out of the scrollable `Column` into Scaffold's `topBar`
   (matching Journal/Help), so the collapsed header stays pinned as a slim icon bar instead of
   scrolling off-screen. `profileScrollState`/`isLandscape`/`collapsed`/`bannerBottomPadding` hoisted
   above `Scaffold`; both nested Columns preserved (no brace imbalance).

**code-brief-adventure-reel-outro-card-v2.md** (`AdventureReelEncoder.kt` + `AppActions.kt` + 1 string ×11):
1. Fix 1 — outro card repositioned to clear the video-player pause-icon zone (frame center y≈640):
   logo y 260→180, title 470→380, tagline 508→416, QR `cardTop` 580→800, `cardPad` 28→24, CTA gap
   56→50; `qrBitmap` 420→360px, `outroLogoBitmap` 160→140px.
2. Fix 2 — 28px fully-opaque monkey glyph drawn inside the location pill (guaranteed contrast on
   busy/light photos); new `pillLogoBitmap` load + recycle, new `pillLogoPaint`, `drawBranding`
   signature gains `pillLogoBitmap` param (updated at BOTH call sites — dissolve loop + main loop).
3. Fix 3 — `AppActions.REEL_SHARE_URL` comment-only: documents why the share caption keeps the full
   play.google.com URL and deliberately does NOT match the QR's short redirect.
4. Tagline copy `reel_outro_tagline` "Your travels, saved" → "Every trip, remembered", re-translated
   across all 11 locales (glyphs verified intact).
- **Supporting assets** (Cowork pre-generated): regenerated `res/drawable-nodpi/reel_qr_code.png`
  (denser v5 code encoding a short `mictroid.github.io/macaco/r/` redirect) + `design/` source, and
  new `r/index.html` static redirect page → full tracked Play URL, deploys via the existing
  push-to-master GitHub Pages pipeline. All committed in this push so the new QR resolves.
- **Build catch (fixed):** the main-display-loop `drawBranding` call site had different indentation
  than the dissolve-loop one, so the initial edit missed it → arg-mismatch; patched, rebuilt clean.

## vc64 / 1.6 SHIPPED — 5-brief batch (reel outro + header/UX/search fixes) — WIF run `29104228834` ✅ CONFIRMED SUCCESS

**✅ CONFIRMED SUCCESS** (~6m41s, 15:35:16→15:41:57 UTC), PUBLISHED to closed testing. Dispatched
**2026-07-10 17:35:15 +02:00** (15:35:16 UTC) → run
[`29104228834`](https://github.com/mictroid/macaco/actions/runs/29104228834). versionCode 63→64,
versionName 1.6. Bump+notes+impl all in one commit `1f1eba4`; **origin == HEAD (`1f1eba4`) verified
before dispatch**. `assembleDebug` BUILD SUCCESSFUL. **✅ ON-DEVICE CONFIRMED 2026-07-10** — user
verified all 5 changes on-device (reel branded card + tracked share link, entry logo centering,
landscape header shrink, accented city search, non-premium backup → paywall). No open items.

## 5-brief batch — implemented — commit `1f1eba4`

Five Cowork briefs, all signatures verified vs live source first. All five → `docs/DONE/`.

**code-brief-entry-detail-header-logo-centering.md** (1 fix):
- `EntryDetailScreen.kt` — replaced the header `Row` + two weighted `Spacer(weight(1f))` with a
  `Box` holding three `Modifier.align`-ed clusters (`CenterStart` back+counter, `Center` brand
  logo, `CenterEnd` share/edit/delete). The weighted-spacer math only centers when the two side
  clusters are equal width (they aren't — ~80dp vs 120dp), so the logo drifted left. No new imports.

**code-brief-landscape-collapsed-header-size.md** (1 fix):
- `MacacoBrandBlock.kt` — collapsed branch now sizes icon + bottom padding off `isLandscape`:
  28dp/4dp in landscape (was fixed 48dp/8dp), portrait unchanged. Fixes "header barely collapses in
  landscape" across all 4 call sites (Journal/Map/Profile/HelpAbout). No new imports.

**code-brief-location-search-diacritics.md** (2 fixes):
- `Cities.kt` — added `Normalizer` NFD-fold (`\p{Mn}+` strip) with a `normalizedCache` computed
  alongside the city cache; `search` compares folded forms; new public `matchesPrefix` helper.
- `NewEditEntryScreen.kt` — past-location filter uses `Cities.matchesPrefix` instead of raw
  `startsWith`. Now "Naxos"→"Náxos", "Fira"→"Firá".

**code-brief-settings-backup-paywall-nav.md** (2 fixes):
- `SettingsScreen.kt` + `NavGraph.kt` — added `onNavigateToPaywall` param wired to
  `Screen.Paywall.route`; Export/Import taps for non-premium users now route to the paywall instead
  of a static Toast. Subtitle/lock-icon "why this is locked" copy unchanged.

**code-brief-adventure-reel-outro-card.md** (Change 1 + 2, + localization):
1. `AdventureReelEncoder.kt` — appended a 2s branded outro card (fade-in 15f + hold 45f) after the
   last photo: dark-teal bg, monkey mark + "macaco" wordmark + tagline, white-carded QR + CTA,
   composited via `saveLayer` so it fades as one unit. New constants/Paints/`loadQrBitmap`/
   `drawOutroCard`; `totalFrames` and `finally` recycle updated. Zero-photo trip still no-ops via
   `?.let` and hits the existing `check(framesRendered > 0)`.
2. `AppActions.kt` + `JournalListScreen.kt` — new `REEL_SHARE_URL` (Play referrer-tagged) added as
   `EXTRA_TEXT` on the reel share intent (+ AppActions import). QR asset
   `res/drawable-nodpi/reel_qr_code.png` + `design/macaco_adventure_reel_qr.png` source committed.
3. 3 new string keys (`reel_outro_tagline`, `reel_outro_cta`, `reel_share_caption`) × 11 locales.

- **Deviation from brief (reel 1b):** reordered `OUTRO_GOLD` / `OUTRO_BG_COLOR` to be declared
  *before* the outro Paint fields. The brief declared them after `outroTitlePaint`, which reads
  `OUTRO_GOLD` in its initializer — Kotlin initializes properties top-to-bottom, so the title paint
  would have got color=0 (transparent), making the "macaco" wordmark invisible on the card. Recorded
  as the recurring "Cowork brief init-order / import mistake" class.
