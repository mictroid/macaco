# Macaco worklog — 2026-06-24

2-brief batch implemented, BUILD SUCCESSFUL. Not yet bumped/pushed/published.

## 2-brief batch (settings-bottom-padding + photo-management) — impl detail

Both Cowork briefs verified against live source before touching any files.

### code-brief-settings-bottom-padding (SettingsScreen.kt)

Verified BEFORE snippet matched line 379 exactly. `navigationBarsPadding` was not yet imported
(brief was correct). Added import after `statusBarsPadding` (line 28); added `.navigationBarsPadding()`
to the scrollable Column modifier. No deviations.

**File:** `ui/screens/SettingsScreen.kt`

### code-brief-photo-management (EntryDetailScreen.kt)

Verified all BEFORE snippets against live source:
- `JournalPhoto` signature at line 646 matched exactly (no `onLongClick` param yet) ✓
- `withCover` at line 637 matched ✓
- `JournalThumb` at line 679 matched ✓
- `photoCount` at line 300 inside `item { }` matched ✓
- `setCover` lambda at line 302 matched ✓

Missing imports verified (not already present): `Star`, `KeyboardArrowLeft`, `KeyboardArrowRight`,
`ListItem`, `ModalBottomSheet`. Added all 5.

Changes made (9 steps from brief):
1. Added `withRemoved` + `withSwapped` extensions after `withCover`
2. Added `PhotoActionSheet` composable after `JournalThumb` (with `@OptIn(ExperimentalMaterial3Api::class)`)
3. Added `photoActionIndex` state var after `listState`
4. Added `PhotoActionSheet` trigger + moved `photoCount` above `LazyColumn`; removed duplicate inside `item { }`
5. Updated `JournalPhoto` to accept optional `onLongClick` (added `@OptIn(ExperimentalFoundationApi::class)`, switched to `combinedClickable`)
6. Wired `onLongClick = { photoActionIndex = 0 }` on single-photo hero
7. Wired `onLongClick = { photoActionIndex = 0 }` on multi-photo hero (left collage slot)
8. Changed right-column `JournalThumb` `onLongClick` from `setCover(index)` → `photoActionIndex = index`
9. Changed overflow-row `JournalThumb` `onLongClick` from `setCover(index)` → `photoActionIndex = index`

`setCover` lambda remains in file (still called indirectly via `PhotoActionSheet.onSetCover`).

4 new string keys × 11 locales via PowerShell BOM script: `entry_detail_photo_set_cover`,
`entry_detail_photo_move_left`, `entry_detail_photo_move_right`, `entry_detail_photo_remove`.
All 44 entries verified correct (incl. CJK/accented chars).

`assembleDebug` BUILD SUCCESSFUL (53s). Briefs moved to `docs/DONE/`.
