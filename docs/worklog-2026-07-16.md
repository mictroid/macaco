# Worklog — 2026-07-16

## IMPLEMENTED, not shipped — vc72 (1.6) — 3-brief batch: subscription renewal info, false-premium fix, weather unit by location

Impl commit `fb38f91`; worklog-rollup commit `c1483c0`. `versionCode` not yet bumped (still 71) —
batching toward the usual 4-5 change release before dispatch. All three briefs verified against
live source first (BEFORE blocks matched exactly, with two real drift points caught and adjusted
for — see below). `assembleDebug` BUILD SUCCESSFUL after fixing two build breaks surfaced by the
verify pass (also noted below). All three briefs → `docs/DONE/`.

- **code-brief-subscription-renewal-info.md** (`BillingManager.kt`, `SubscriptionInfoScreen.kt`,
  `RevenueCatConfig.kt`, `JournalViewModel.kt`, `MainActivity.kt`, `NavGraph.kt`, new
  `RenewalReminderScheduler.kt` + `RenewalReminderWorker.kt`): exposes
  `BillingManager.currentExpirationDate: StateFlow<Long?>`; `SubscriptionInfoScreen` now shows
  "Renews on `<date>` — `<price>`" beneath the plan label for subscribers; new WorkManager
  one-time job posts a notification 7 days before an **annual** subscriber's renewal (gated on
  `RevenueCatConfig.ANNUAL_BASE_PLAN_ID`, rescheduled on every entitlement refresh), deep-linking
  back into Subscription Info via new `MainActivity.ACTION_OPEN_SUBSCRIPTION`. Monthly subscribers
  get no renewal notification, by design (brief's explicit scope). **Deviation:** the brief didn't
  spell out the `com.houseofmmminq.macaco.util.RenewalReminderScheduler` import needed in
  `BillingManager.kt` (different package) — added it; no other changes from spec.
- **code-brief-subscription-screen-false-premium.md** (`ProfileScreen.kt`,
  `SubscriptionInfoScreen.kt`, `NavGraph.kt`, strings ×11): Profile's "Subscription" tile (both
  wide/phone layouts) now gates on `isPurchased`, matching the existing Print Book pattern in
  `SettingsScreen.kt` — previously a free account could open the screen unconditionally and see a
  false "Premium — Active — Lifetime" band (the plan-label `when` defaulted to "Lifetime" whenever
  nothing matched). `SubscriptionInfoScreen` gained a real non-premium early-return state with an
  "Upgrade to Premium" CTA routing to `Screen.Paywall.route`. No deviations — all BEFORE blocks
  matched exactly, including the already-existing `Screen.Paywall.route`.
- **code-brief-weather-unit-by-location.md** (`WeatherLookup.kt`, `TravelEntry.kt`,
  `JournalViewModel.kt`, `EntryDetailScreen.kt`): `WeatherLookup.geocode()` now also returns the
  geocoded `Address.countryCode`; `Result` gains `isFahrenheit` (true only for `"US"`), decided
  once at fetch time from the **entry's own location**, not `Locale.getDefault().country` (device
  display-language setting) — fixes a Berlin entry reading °F on a phone set to English (US).
  Persisted as new `TravelEntry.weatherIsFahrenheit: Boolean?` field; old entries with no value
  fall back to metric (°C) at both `EntryDetailScreen` chip call sites. No new strings. No
  deviations.

**Build breaks caught by the verify pass (not brief errors — encoding/language-level issues found
while implementing):**
1. `values-fr/strings.xml` — `renewal_channel_name` ("Renouvellement de l'abonnement") had an
   unescaped apostrophe, which `aapt` rejected as an invalid Unicode escape. Fixed to `l\'abonnement`.
2. `SubscriptionInfoScreen.kt` — `currentExpirationDate` (a `by ... collectAsState()` delegated
   property) couldn't smart-cast to non-null `Long` inside the `if (... != null)` block per
   Kotlin's delegated-property smart-cast rules. Fixed by binding it to a local
   `val expirationMillis = currentExpirationDate` first, then null-checking that.

**Localization:** 9 new string keys (`subscription_renews_on`, `subscription_renews_on_with_price`,
`renewal_channel_name`, `renewal_channel_desc`, `renewal_notification_title`,
`renewal_notification_body`, `subscription_not_premium_title`, `subscription_not_premium_body`,
`subscription_upgrade_cta`) inserted across all 11 locales via the BOM-script recipe, anchored
after `subscription_plan_recurring`. Verified via grep — correct glyphs (CJK, accents, em dash) in
every locale.

**Not yet done:** on-device verify pass (per the `verify-on-s8-before-ship` workflow) and Play
dispatch — holding for more changes per the user's "sum up four or five before we push" instruction.

---

## IMPLEMENTED, not shipped — vc72 (1.6) — email verification gate

**code-brief-email-verification.md** (`UserProfile.kt`, `AuthRepository.kt`,
`FirebaseAuthRepository.kt`, `MockAuthRepository.kt`, `JournalViewModel.kt`, `NavGraph.kt`, new
`VerifyEmailScreen.kt`, strings ×11): email/password signups now must click the Firebase
verification link before entering the journal. `UserProfile.emailVerified: Boolean = true` (default
keeps Google accounts, which are pre-verified, and any indifferent call site unaffected);
`createAccount()` calls `sendEmailVerification()` right after account creation;
`toUserProfile()` maps `emailVerified = provider == Google || isEmailVerified`. New
`AuthRepository.sendEmailVerification()` (resend) / `reloadAndCheckEmailVerified()` (refreshes
Firebase state and manually pushes it into `_currentUser`, since `reload()` doesn't fire the auth
listener) — mirrored in `MockAuthRepository` (new email/password mock accounts start unverified;
the second `reloadAndCheckEmailVerified()` call flips them verified, so the flow is testable without
Firebase). `NavGraph`'s gating `when` gained a
`currentUser?.emailVerified == false -> VerifyEmailScreen(...)` branch between the "not logged in"
and "full journal" cases. Verified against live source first — signatures, imports, and the real
(more elaborate, multi-gate) `NavGraph` structure all matched the brief's simplified BEFORE/AFTER
once the elided gates (onboarding/splash/app-lock) were accounted for.

**Deviation (real bug in the brief's own code sample, not a stale-repo drift):** the brief's
`VerifyEmailScreen.kt` snippet referenced `it.getString(R.string.verify_email_still_unverified)`
inside the `onSuccess = { verified -> ... }` fold branch — `it` there is the `Boolean` result, which
has no `getString`. Fixed by adding `val context = LocalContext.current` (matching `LoginScreen`'s
existing pattern) and calling `context.getString(...)`. Also the brief fully-qualified the email
icon as `androidx.compose.material.icons.Icons.Filled.Email`, which doesn't compile — `Icons.Filled.Email`
is a Kotlin extension property, not a nested member, so full qualification doesn't resolve it; fixed
by importing `androidx.compose.material.icons.Icons` + `androidx.compose.material.icons.filled.Email`
and using the plain reference (same style as `LoginScreen.kt`/`HelpAboutScreen.kt`, which already use
this icon). Caught both by the delegated `assembleDebug` build failing on the first pass; fixed here
and re-verified BUILD SUCCESSFUL.

**Localization:** 7 new string keys (`verify_email_title`, `verify_email_subtitle`,
`verify_email_continue`, `verify_email_still_unverified`, `verify_email_resend`,
`verify_email_resend_cooldown`, `verify_email_sign_out`) added across all 11 locales via the
bulk-localize-strings skill (translated per-locale, not copied English).

---

## IMPLEMENTED, not shipped — vc72 (1.6) — security hardening: versioned Firestore rules + Firebase App Check

Two briefs from `docs/security-review-2026-07-16.md` (findings M1/M2/M3). Verified against live
source first: confirmed no `firestore.rules`/`firebase.json` existed yet, confirmed the `M3`
`/shared_trips` finding's wording against the security review, and confirmed the app-check brief's
two real drift points below before implementing.

- **code-brief-security-firestore-rules-in-repo.md** — new `firestore.rules` (repo root) capturing
  the live per-user isolation rule (`match /users/{userId}/{document=**} { allow read, write: if
  request.auth != null && request.auth.uid == userId; }`) **without** the orphaned
  `/shared_trips` public-read block (leftover from the retired trip-share feature,
  `docs/DONE/code-brief-retire-trip-share-link.md`; flagged as M3 in the security review — grants
  unauthenticated read of any `shared_trips/{shareId}` doc). New `firebase.json` pointing
  `firestore.rules` at the deploy path. **No app code changes; no deviations** — Changes 1–2 only.
  **Change 3 (deploying the rules + confirming the orphaned block is gone in the console) is the
  owner's step** — publishes to the live `macaco-499016` project, not run here.
- **code-brief-security-app-check.md** — adds Firebase App Check (Play Integrity provider for
  release, debug provider for debug builds) as the code-side mitigation for the extractable-API-key
  findings M1/M2: `firebase-appcheck-playintegrity` + `firebase-appcheck-debug` deps added to
  `gradle/libs.versions.toml` / `app/build.gradle.kts`; provider installed in
  `TravelJournalApp.onCreate()`. **Deviations (both real gaps in the brief, not stale-repo drift,
  caught by verifying the target file before editing):**
  1. `TravelJournalApp.kt` had **no `onCreate()` override at all** — the brief assumed one existed
     to add code "near the top of" — added a new override calling `super.onCreate()` then
     installing the App Check provider.
  2. `app/build.gradle.kts`'s `buildFeatures` block didn't have `buildConfig = true` — without it
     `BuildConfig.DEBUG` (which the brief's snippet reads to pick the provider) wouldn't have
     compiled. Added it.
- **On-device S8 verify (per `verify-on-s8-before-ship`):** installed the rebuilt debug APK fresh
  (`adb install -r`), walked onboarding → login → signed in with a real account → journal list
  loaded (existing Firestore-synced entries rendered, confirming reads still work under the
  `users/{uid}/**` rule + App Check) → opened an entry, edited it, tapped Save (a real Firestore
  write) → change persisted, photos re-synced from Drive, no crash. Logcat confirmed: App Check's
  `DebugAppCheckProvider` printed its debug secret on launch (needs registering in the console —
  Change 3 of the app-check brief, owner's step), no `FATAL`/`AndroidRuntime`/`PERMISSION_DENIED`/
  `AppCheck`-rejection lines anywhere in the session. The RevenueCat
  `ClassNotFoundException: com.amazon.device.iap.PurchasingListener` line seen at launch is
  pre-existing, benign (RevenueCat's own Amazon-store reflection probe) and unrelated to this batch.
- **Correction — Change 3 (deploy) already done:** `docs/security-infra-changes-2026-07-16.md`
  (written by Cowork, discovered after the commit below) confirms the Firestore rules were already
  **updated + published** in the console this same session, `/shared_trips` block removed, rules
  text matching `firestore.rules` exactly. That doc also records unrelated console-only hardening
  from the same session (Google Maps API key migrated into `macaco-499016` + restricted to Android
  apps, `local.properties`'s `MAPS_API_KEY` value rotated — git-ignored, no code change — and the
  GitHub Actions `MAPS_API_KEY` secret updated to match; Firebase Android API key restricted to the
  three registered SHA-1s). No code changes needed for any of that.
- **Still open (owner's step):** register the app + debug token in Firebase console → App Check,
  leave Firestore/Auth enforcement in **monitor mode** for a release cycle or two before flipping
  enforce.
