# Macaco worklog — 2026-06-27

2-brief batch implemented, BUILD SUCCESSFUL. Not yet bumped/pushed/published.
Verified vc31/36/37 working on-device (user) except map-camera fit — which this batch fixes.

## 2-brief batch (dark-mode-card-borders + map-camera-v6) — impl detail

Both Cowork briefs verified against live source before touching any files.

### code-brief-dark-mode-card-borders (JournalListScreen.kt)

Verified both BEFORE snippets matched live source exactly: `EntryCard` Card block at line 773,
`OnThisDayCard` Card block at line 1008. Both imports (`androidx.compose.foundation.border`,
`androidx.compose.ui.graphics.luminance`) confirmed NOT already present (brief was correct) — added
both. No deviations.

Changes:
- Added `androidx.compose.foundation.border` import (after `.background`) and
  `androidx.compose.ui.graphics.luminance` import (after `ui.graphics.Color`).
- `EntryCard`: added `val isDark = MaterialTheme.colorScheme.background.luminance() < 0.5f` and a
  conditional `0.5.dp outlineVariant` border (16.dp shape) applied only in dark mode via
  `.then(if (isDark) Modifier.border(...) else Modifier)`.
- `OnThisDayCard`: same treatment with the 12.dp shape.

Light mode unchanged (the `isDark` guard keeps a bare `Modifier`). Works across all 7 themes since
the check reads the rendered scheme, not the system flag.

**File:** `ui/screens/JournalListScreen.kt`

### code-brief-map-camera-v6 (MapScreen.kt)

Verified the BEFORE block matched live source (the body of the `if (mapLoaded && ...)` guard inside
`LaunchedEffect(mapLoaded, geocodingComplete)`, lines 144–177): `latlngs` build + `buildUpdate()`
zoom-table + `delay(100)` + `runCatching { ... move(buildUpdate()) }`. Confirmed `entries` StateFlow
in scope (line 89).

Change: replaced the bounds-fitting + zoom-table block with the most-recent-entry approach —
`entries.sortedByDescending { createdAt }` → first geocoded, non-Null-Island location →
`CameraUpdateFactory.newLatLngZoom(target, 6f)`. Fixes the cross-continental regression where a
>90° lng span fell through the zoom table to `2f` (useless world view). `cameraPositioned` gate
preserved; the 8s `revealTimedOut` safety net, scrim, and marker rendering untouched.

Import cleanup (brief's optional step): removed `com.google.android.gms.maps.CameraUpdate` and
`com.google.android.gms.maps.model.LatLngBounds` — both were referenced ONLY inside the deleted
`buildUpdate()`. Verified via grep they have no other use. `delay` import kept (still used by the
8000ms `revealTimedOut` in the sibling LaunchedEffect).

**File:** `ui/screens/MapScreen.kt`

## Verification

`assembleDebug` BUILD SUCCESSFUL (2m9s). Installed on the **Pixel 8 emulator** (`emulator-5554`) and
launched — reached the login screen (forgot-password link from vc36 visible). Could NOT visually
verify the two journal-internal screens: the emulator is a fresh profile behind the login + premium
gate, and the real RevenueCat key means a sideloaded debug APK can't get past the paywall. Per user
decision, **visual verification deferred to the A53 after this ships** to closed testing (same as
prior batches). Briefs moved to `docs/DONE/`.

## vc38 / 1.6 SHIPPED — dispatched 2026-06-27

Bumped versionCode 37→38 (versionName stays 1.6). Refreshed `default.txt` (map opens on most recent
adventure / dark-mode card outline). Brief batch in `2335322`; bump + notes in `f00a785`. Pushed
`3cf4ede..f00a785`; **origin == local HEAD (`f00a785`) verified before dispatch.**

WIF release.yml dispatched → **run `28286360921`, dispatched 2026-06-27 12:20:21 local (+02:00);
confirmed SUCCESS** (5m56s, completed 10:26:18 UTC). **vc38 PUBLISHED to closed testing.** Not
device-verified — A53 after publish.
https://github.com/mictroid/macaco/actions/runs/28286360921

Per the standing rule (2026-06-27), the ship summary was emailed to the user (Gmail draft).

## On-device verify of vc38 → map fix (map-camera-v7) + vc39 SHIPPED

User installed vc38 from Play and sent two A53 screenshots:
- **dark-mode-card-borders — CONFIRMED working.** Journal cards + section cards show the subtle
  outline against the dark background. ✅
- **map-camera-v6 — worked exactly as specified but the spec was wrong.** v6 centered on the single
  most-recent entry (Patagonian Glacier Trek → El Calafate, zoom 6); header read "7 of 7 locations
  mapped" so all pins exist, camera just parked on the newest. User wants the map to **frame all
  travels** (the pre-regression behaviour). Confirmed via AskUserQuestion: "Fit all my pins."

**map-camera-v7** (user-directed, supersedes v6; commit `af5b99c`): replaced the single-target
`newLatLngZoom` with the **`newLatLngBounds(bounds, width, height, padding)` overload** — explicit
pixel dimensions (`context.resources.displayMetrics`) + 48dp padding. This overload does NOT require
the map to be laid out (so no "Map size can't be 0" throw the v3–v5 saga fought) AND computes the
exact zoom to fit every pin (no manual zoom-table that collapsed cross-continental spans to zoom-2).
Single-location falls back to `newLatLngZoom(.,6f)`. Re-added the `LatLngBounds` import (removed in
v6). `cameraPositioned` gate + 8s `revealTimedOut` net unchanged. `assembleDebug` BUILD SUCCESSFUL.

**vc39 / 1.6 SHIPPED:** bump vc38→39 + refreshed `default.txt` (map frames all travels) in `38713db`.
Pushed `b7dbd51..38713db`; **origin == HEAD (`38713db`) verified before dispatch.** WIF release.yml
dispatched → **run `28287898388`, dispatched 2026-06-27 13:30:51 local (+02:00)**. Not yet confirmed
(~6 min). A53 verify owed: map should now frame ALL pins on open.
https://github.com/mictroid/macaco/actions/runs/28287898388
