# Worklog — 2026-07-17

## vc73 (1.6) — 3-brief batch implemented + committed, NOT yet shipped

**versionCode:** 73 · **versionName:** 1.6 · **HEAD commit:** `134467e` (5 commits ahead of
`origin/master` `29316d5`, unpushed).

Session recovered a prior vc73 session that was cut off by Anthropic API 529 ("overload") errors
mid-work — **not** a build or Drive failure. The Drive-sync brief's code was already correctly in
the working tree; verified it, then implemented the remaining two briefs.

### Commit `134467e` — final 3-brief batch

Three Cowork briefs, all from `docs/qa-report-2026-07-17.md` findings (moved to `docs/DONE/`):

1. **code-brief-drive-sync-status-accuracy** (`DrivePhotoSync.kt`)
   - `syncAll` now awaits `downloadMissing(entries)` before flipping `_syncState` to `Synced` on
     both completion paths (no-pending-uploads path + post-upload-loop path), so "Synced" no longer
     appears while photos are still downloading.
   - `downloadMissing` counts per-file download failures via an `AtomicInteger` across both the
     photo and video loops (failure = Drive call threw AND cache file still absent) and sends one
     aggregate snackbar through the existing `_errors` channel.
   - No deviations — the working-tree code matched the brief exactly.

2. **code-brief-helpabout-widget-drive-faq** (`HelpAboutScreen.kt` + `strings.xml` ×11)
   - 4 new FAQ Q/A pairs: home-screen widget (incl. the App-Lock-doesn't-hide-it nuance), Drive
     orphan behavior on entry/account delete, camera-roll suggestion banner, email-verification gate.
   - 8 new `help_faq_*` keys added to all 11 locales; a renewal-date/reminder sentence appended to
     the existing `help_faq_a_billing` value in all 11 (appended, not replaced — Grep-verified).
   - Bulk localization done via the `bulk-localize-strings` skill recipe (BOM-on-script,
     no-BOM-on-XML PowerShell). No deviations.

3. **code-brief-repo-housekeeping-vc73** (`app/build.gradle.kts`, `gradle/libs.versions.toml`,
   `CLAUDE.md`, `.gitignore`)
   - Dropped the unused `firebase-storage` dependency + its toml alias (no `FirebaseStorage` imports
     anywhere; R8 release build confirmed clean without it).
   - Rewrote the stale NavGraph gate diagram in `CLAUDE.md` to match live `NavGraph.kt`
     (onboarding/splash/app-lock/verify-email gates, freemium with `FREE_ENTRY_LIMIT = 3`, bottom
     tabs).
   - **Deviation — Change 1 (S1, launcher icons) was stale/phantom:** the QA report flagged
     uncommitted launcher-icon edits, but re-verifying the live tree showed no such modification
     (only the intended code was dirty). No action taken — consistent with the
     `phantom-corruption-brief` memory. The untracked-file sweep was resolved per Michael's call:
     `.gitignore` the local QA/device-dump assets (`docs/screenshots/Screenshot_*.jpg`, `NN/`
     folders, `*.mp4`, `docs/play-store-feature-images/`, `docs/qa-report-*.md`) and the two root
     working files (`Macaco-App-Names-Update-*.docx`, `app-qa.skill`) — not committed.

### Build verification
`assembleDebug` and `assembleRelease` both pass (R8 clean without firebase-storage). Localization
verified via Grep: 11 hits for each new key, CJK/accented glyphs intact, `help_faq_a_billing`
retains original copy + appended sentence in every locale.

### Full vc73 batch (5 unpushed commits)
`c2c390d` (R8 ComponentRegistrar keep-rule fix) → `83571a1` (vc73 bump + release notes) →
`14b3b31` (toolchain AGP 9.3.0 + Gradle 9.5.0) → `7f8847d` (shared Firestore mapper — restores the
weather-unit feature) → `134467e` (this 3-brief batch).

### Open before dispatch
- Restore the weather bullet in Play release notes (`default.txt`) — it was pulled when the weather
  claim was briefly false; the mapper brief (`7f8847d`) makes it real again.
- On-device S8 verify pass (FAQ rendering + Drive-sync behavior) per `verify-on-s8-before-ship`.
- Not pushed / not dispatched.

## vc73 SHIPPED — dispatched to Play (Open testing)

- **Pre-dispatch resolved:** weather bullet restored in `default.txt` (`24d0c88`); on-device S8
  verify done — including the mandatory **R8 release-APK launch test** (per `r8-release-launch-test`):
  `app-release.apk` (minified) launches clean past `TravelJournalApp.onCreate`, Crashlytics 17.2.1
  initializes, zero `Could not instantiate` / registrar failures. The vc72 launch-crash is confirmed
  fixed on the real R8 build under AGP 9.3.0 + firebase-storage drop.
- **origin == HEAD verified:** both `24d0c88` before dispatch.
- **Dispatched:** WIF run `29596794584`, `2026-07-17 18:36:01 +0200`
  (https://github.com/mictroid/macaco/actions/runs/29596794584). Manual dispatch, not watched.
- **versionCode 73, versionName 1.6.** Track = `beta` (Open testing).
- **Status:** dispatched, NOT confirmed — check with `gh run list` (~6 min build).
- **Remaining caveat:** Drive-sync behavioral change (await-download-before-Synced) not
  deterministically exercised on-device; code-reviewed + both builds green.
