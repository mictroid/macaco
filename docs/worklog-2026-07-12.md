# Worklog — 2026-07-12

## Implemented (shipped in vc68) — 6-brief polish batch (Cowork drop)

Six Cowork briefs implemented in one session, all verified vs live source first (every BEFORE block
matched exactly), new strings localized across 11 locales, `assembleDebug` BUILD SUCCESSFUL. All 6
→ `docs/DONE/`. Installed on the S8+ (`ce03171353d9547e0c`); shipped per user instruction without a
separate on-device pass. Impl commit `<IMPL>`; bump+notes `<BUMP>`.

- **print-pdf-blank-images-fix** (`data/sync/PrintBookExporter.kt`): `decodeForPrint()`'s bounds
  read chained an elvis off `BitmapFactory.decodeStream(..., inJustDecodeBounds=true)`, which
  **always returns null by design** — so every photo bailed `return@runCatching null` and every
  print-book page rendered the gray placeholder. (This is the *actual* cause of the "all
  placeholders" seen in vc67, which was misdiagnosed there as a test-data/Drive-account artifact.)
  Fix: guard on the stream opening, run the bounds-decode for its side effect, then check
  `rawW/rawH` — matching `JournalBackup.compressToBytes`'s proven pattern.

- **journal-fab-inset-and-suggestion-grouping** (`ui/screens/JournalListScreen.kt`, strings):
  (1) FAB inset narrowed from `WindowInsets.safeDrawing` to
  `navigationBars.union(displayCutout)` — `safeDrawing` also bundles IME + system-gesture insets,
  which pushed the FAB to mid-screen on the S8; the narrower union still covers the original A53
  side-nav-bar case. (2) Camera-roll suggestion cards sharing a `placeName` now collapse into one
  `SuggestionGroupCard` (new composable) with per-visit Dismiss/Create rows, instead of repeating
  near-identical cards. (3) "1 photos" → "1 photo": new `photo_suggestion_photo_count` /
  `photo_suggestion_visit_count` plurals; `photo_suggestion_subtitle` placeholder `%1$d`→`%1$s`
  (now receives a pre-pluralized phrase). **Deviation:** the brief's `remember(suggestedClusters){…}`
  was called inside `LazyListScope` (not a `@Composable` context — compile error); replaced with a
  plain `groupBy` (cheap, runs during list composition).

- **reel-icon-placement-and-collapsible-groups** (`data/sync/AdventureReelEncoder.kt`,
  `ui/screens/JournalListScreen.kt`, strings): (1) Reel location-pill glyph 28→34px and anchored to
  the CENTER-aligned text's measured left edge (`measureText`, clamped ≥40f) so icon+text read as
  one group. (2) Collapsible trip/month groups on the journal list: session-scoped
  `collapsedSections: Set<String>` + `toggleSection`, chevron + clickable row on `TripHeader` /
  `MonthHeader`, `items(...)` skipped when collapsed. Shares `common_expand`/`common_collapse` with
  the Help & About brief (added once).

- **year-in-travel-recap-v2** (`ui/screens/YearInTravelScreen.kt`, `ui/screens/ProfileScreen.kt`):
  branded the in-app screen to match the share export — teal `macacoBrandBackground()` header band
  + `MacacoBrandBlock`, big gold year numeral, stat grid in an elevated card with `SplashGold`
  values, gold+medium highlight lines, brand mark above Share. `ProfileScreen.StatItem` gained
  additive `valueColor`/`labelColor` params (default `Color.Unspecified`/`onSurfaceVariant` — Profile's
  own stats card unchanged). No new strings/assets.

- **help-about-collapsible-and-new-features** (`ui/screens/HelpAboutScreen.kt`, strings):
  collapsible FAQ sections (session-scoped `collapsedSections: Set<Int>`), `SectionHeader` gains
  optional icon + chevron/toggle, `FaqSection` gains `icon: ImageVector` (themed icon per section);
  version label pinned to the header `Box` top-end (visible in all header states) and dropped from
  the portrait-only branch; 3 new sections (Print & Export, Year in Travel, Trip Sharing) + 2 Q&As
  folded into Getting Started (Search, Weather). Only the icons actually used were imported (Search /
  WbSunny from the brief's ADD list are unused — omitted). 3 section titles + 10 Q&A strings ×11.

- **profile-avatar-top-spacing** (`ui/screens/ProfileScreen.kt`): added `Spacer(Modifier.height(14.dp))`
  before the avatar Box in the signed-in identity Column so the avatar isn't glued to the banner
  seam. Flat value (not tied to `bannerBottomPadding`), signed-out branch untouched.

**Localization:** `common_expand`/`common_collapse` (once, shared), `photo_suggestion_photo_count` /
`photo_suggestion_visit_count` plurals, `photo_suggestion_subtitle` placeholder change, 3 help
section titles + 10 help Q&A strings — all ×11 locales (values + de/es/fr/it/ja/nl/pl/pt/sv/zh-rCN).
Inserted via the BOM-script recipe; glyphs verified intact (CJK, accents, `&amp;`). `pl` plurals use
one/few/many/other; ja/zh use other-only.
