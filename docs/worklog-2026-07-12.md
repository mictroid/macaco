# Worklog — 2026-07-12

## SHIPPED — vc70 / 1.6 — vc69 review-fixes batch (4 fixes from vc69 screenshot review) — WIF run `29200464388` ✅ CONFIRMED SUCCESS

Dispatched **18:37:13 +0200 / 16:37:13 UTC**, confirmed success (6m47s) —
https://github.com/mictroid/macaco/actions/runs/29200464388. Impl commit `5bb5d7b`; bump+notes
`f4e4edd` (vc69→70, versionName 1.6 unchanged). **origin == HEAD (`f4e4edd`) verified before
dispatch.** `assembleDebug` BUILD SUCCESSFUL. Shipped per user instruction ("push and ship") without
a separate on-device pass. **PUBLISHED to closed testing.**

`code-brief-vc69-review-fixes.md` → `docs/DONE/`. All four changes verified against live source
first (exact BEFORE-block matches); no deviations.

- **Change 1 — stale entitlement read** (`BillingManager.kt`, `MainActivity.kt`): `refreshEntitlement()`
  made non-private; `MainActivity.onResume()` now calls
  `(application as TravelJournalApp).billingManager.refreshEntitlement()` alongside the existing
  `checkForUpdate()`, so a stale premium read (missing reel icon / Drive gate / Print Book) self-heals
  within a resume cycle instead of persisting for the process lifetime.
- **Change 2 — Drive backup not actually gated** (`JournalViewModel.kt`, `SettingsScreen.kt`): gated
  `saveEntry`'s photo/video upload, the entries-collect auto-download loop, and
  `syncPhotosToGoogleDrive()` behind `isPurchased.value == true`. `DriveBackupCard` now shows a lock
  icon and error-tinted messaging whenever `!premium` (previously only when not-connected-and-not-
  premium), and hides "Sync Now" (keeping only Disconnect) when connected-but-not-premium — a lapsed
  subscriber can disconnect but can't keep syncing free. No new strings (`settings_drive_premium_required`
  already existed ×11).
- **Change 3 — Year in Travel recolor** (`YearInTravelScreen.kt`): stat grid numbers all → teal
  (`colorScheme.primary`), labels all → gold (`SplashGold`, reversed from before), card gained a
  1.5dp teal `BorderStroke`. Added `BorderStroke` import.
- **Change 4 — duplicate branding removed** (`YearInTravelScreen.kt`): deleted the second
  `MacacoBrandBlock(collapsed = true)` + standalone `"macaco"` text block above the Share button (top
  header already provides the branding). Dropped the now-unused `androidx.compose.ui.unit.sp` import
  (confirmed `.sp` had no other use in the file — `FontWeight`/`TextAlign` imports still used
  elsewhere, kept).

## SHIPPED — vc69 / 1.6 — 4-brief batch (Help/Year/trip-share/premium-gating) — WIF run `29192148641`

Dispatched **14:10:17 +0200 / 12:10:17 UTC** —
https://github.com/mictroid/macaco/actions/runs/29192148641. Impl commit `fef7cb4`; bump+notes
`5b84267` (vc68→69, versionName 1.6). **origin == HEAD (`5b84267`) verified before dispatch.**
`assembleDebug` BUILD SUCCESSFUL. Shipped per user instruction ("build and ship") without a separate
on-device pass. **Dispatched, not yet confirmed** — check `gh run list` (~6 min). All BEFORE blocks
verified vs live source first. Four briefs → `docs/DONE/`; a fifth
(`fix-truncated-uncommitted-files`) was investigated and **correctly skipped as a phantom** — see below.

- **help-about-and-year-in-travel-fixes** (`ui/screens/HelpAboutScreen.kt`,
  `ui/screens/YearInTravelScreen.kt`): (1) FAQ sections now open **collapsed** by default
  (`collapsedSections` seeded with every `FAQ_SECTIONS.titleRes`). (2) Version label moved from the
  pinned top-right corner `Text` to a centered line **under "macaco"** in both portrait and landscape
  header branches (landscape wrapped in a `Column`; portrait tagline "Roam Freely…" replaced with the
  `help_title` page title per Change 3), hidden when collapsed. (3) Year in Travel: added
  `verticalScroll` + `navigationBarsPadding` so the Share button clears the system nav bar (A53) and
  landscape scrolls. No new strings.

- **retire-trip-share-link** (`ui/screens/JournalListScreen.kt`, `ui/viewmodel/JournalViewModel.kt`,
  deleted `data/sync/TripShareManager.kt`, `ui/screens/HelpAboutScreen.kt`): removed the blocked
  `/shared_trips` link-share feature entirely — dropped the Share icon button from `TripHeader`
  (Reel-only now), the `onShare` param + call-site wiring, `shareDialogTrip` state, the
  `ShareTripDialog`/`ExpiryOption` composables, `TripShareState`/`createTripShare`/`revokeTripShare`/
  `tripShareConsumed` + the `TripShareManager` import, and the "Trip Sharing" `FaqSection`. Deleted
  `TripShareManager.kt`. Also removed the now-dead `Icons.Filled.Share` import from both screens.
  **Deviation:** skipped the optional Change-5/6 pruning of the 13 unused `trip_share_*` + 3
  `help_*_trip_sharing` string keys (dead strings are harmless; avoided 11-locale churn) — noted for a
  future cleanup pass.

- **premium-gating-corrections** (`ui/screens/SettingsScreen.kt`, strings ×11): gated Google Drive
  photo backup behind purchase — `DriveBackupCard` gained a `premium` param, call site passes
  `isPurchased == true` and routes `onConnect` to `onNavigateToPaywall()` when unpurchased; new
  premium-required subtitle (error-colored) when not purchased. New string `settings_drive_premium_required`
  ("A Premium feature — unlock it from Subscription") ×11. Rewrote `help_faq_premium_benefits_a` to
  stop overclaiming map/swipe/themes as premium (they stay free) and list the actually-gated features;
  appended a Drive-backup-is-premium sentence to `help_faq_a_backup`. All ×11 locales.

- **year-in-travel-teal-gold** (`ui/screens/YearInTravelScreen.kt`): year numeral + Trips/Locations/
  Media stats recolored to `colorScheme.primary` (theme-aware teal); Memories stays `SplashGold` (hero
  stat). Mood/tag/busiest-month converted from stacked left-aligned text to centered `titleMedium` pill
  chips via new `YearRecapHighlightChip` (Surface, `primaryContainer`, icon+label) in a `FlowRow`
  (`@ExperimentalLayoutApi`). Full-teal-background option deprioritized per brief. **Deviation:**
  replaced the trailing `Spacer(Modifier.weight(1f))` with `Spacer(Modifier.height(24.dp))` — `weight`
  is invalid inside the `verticalScroll` column added by the help/year brief above (infinite-height
  constraint); the fixed spacer lets the footer flow correctly. Added `size` import (chip uses it).

- **fix-truncated-uncommitted-files** — **PHANTOM, no action.** This brief claimed my in-progress
  edits had truncated `strings.xml` / `JournalListScreen.kt` / `HelpAboutScreen.kt` /
  `YearInTravelScreen.kt` mid-token. Verified directly against the actual working tree: all four files
  end cleanly (`</resources>` / matched braces), all 7 strings it claimed were "collaterally deleted"
  are present, and there is no `<string name="com` truncation marker. The Edit-tool replacements were
  clean; the corruption never existed. Applying the brief would have duplicated strings and corrupted
  healthy files. Moved to `docs/DONE/` (resolved) with this explanation rather than left flagging.

## SHIPPED — vc68 / 1.6 — 6-brief polish batch (Cowork drop) — WIF run `29184135246`

Dispatched **09:22:28 +0200 / 07:22:27 UTC** —
https://github.com/mictroid/macaco/actions/runs/29184135246. Impl commit `3428710`; bump+notes
`a646fbc` (vc67→68, versionName 1.6). **origin == HEAD (`a646fbc`) verified before dispatch.**
`assembleDebug` BUILD SUCCESSFUL; installed on the S8+ (`ce03171353d9547e0c`). Shipped per user
instruction without a separate on-device pass. **✅ CONFIRMED SUCCESS** (job `86627086634`, 6m52s) —
PUBLISHED to closed testing.

Six Cowork briefs implemented in one session, all verified vs live source first (every BEFORE block
matched exactly), new strings localized across 11 locales. All 6 → `docs/DONE/`.

- **print-pdf-blank-images-fix** (`data/sync/PrintBookExporter.kt`): `decodeForPrint()`'s bounds
  read chained an elvis off `BitmapFactory.decodeStream(..., inJustDecodeBounds=true)`, which
  **always returns null by design** — so every photo bailed `return@runCatching null` and every
  print-book page rendered the gray placeholder. (This is the *actual* cause of the "all
  placeholders" seen in vc67, which was misdiagnosed there as a test-data/Drive-account artifact.)
  Fix: guard on the stream opening, run the bounds-decode for its side effect, then check
  `rawW/rawH` — matching `JournalBackup.compressToBytes`'s proven pattern.

- **journal-fab-inset-and-suggestion-grouping** (`ui/screens/JournalListScreen.kt`, strings):
  (1) FAB inset narrowed from `WindowInsets.safeDrawing` to
  `navigationBars.union(displayCutout)` — `safeDrawing` also bundles IME + system-gesture insets,
  which pushed the FAB to mid-screen on the S8; the narrower union still covers the original A53
  side-nav-bar case. (2) Camera-roll suggestion cards sharing a `placeName` now collapse into one
  `SuggestionGroupCard` (new composable) with per-visit Dismiss/Create rows, instead of repeating
  near-identical cards. (3) "1 photos" → "1 photo": new `photo_suggestion_photo_count` /
  `photo_suggestion_visit_count` plurals; `photo_suggestion_subtitle` placeholder `%1$d`→`%1$s`
  (now receives a pre-pluralized phrase). **Deviation:** the brief's `remember(suggestedClusters){…}`
  was called inside `LazyListScope` (not a `@Composable` context — compile error); replaced with a
  plain `groupBy` (cheap, runs during list composition).

- **reel-icon-placement-and-collapsible-groups** (`data/sync/AdventureReelEncoder.kt`,
  `ui/screens/JournalListScreen.kt`, strings): (1) Reel location-pill glyph 28→34px and anchored to
  the CENTER-aligned text's measured left edge (`measureText`, clamped ≥40f) so icon+text read as
  one group. (2) Collapsible trip/month groups on the journal list: session-scoped
  `collapsedSections: Set<String>` + `toggleSection`, chevron + clickable row on `TripHeader` /
  `MonthHeader`, `items(...)` skipped when collapsed. Shares `common_expand`/`common_collapse` with
  the Help & About brief (added once).

- **year-in-travel-recap-v2** (`ui/screens/YearInTravelScreen.kt`, `ui/screens/ProfileScreen.kt`):
  branded the in-app screen to match the share export — teal `macacoBrandBackground()` header band
  + `MacacoBrandBlock`, big gold year numeral, stat grid in an elevated card with `SplashGold`
  values, gold+medium highlight lines, brand mark above Share. `ProfileScreen.StatItem` gained
  additive `valueColor`/`labelColor` params (default `Color.Unspecified`/`onSurfaceVariant` — Profile's
  own stats card unchanged). No new strings/assets.

- **help-about-collapsible-and-new-features** (`ui/screens/HelpAboutScreen.kt`, strings):
  collapsible FAQ sections (session-scoped `collapsedSections: Set<Int>`), `SectionHeader` gains
  optional icon + chevron/toggle, `FaqSection` gains `icon: ImageVector` (themed icon per section);
  version label pinned to the header `Box` top-end (visible in all header states) and dropped from
  the portrait-only branch; 3 new sections (Print & Export, Year in Travel, Trip Sharing) + 2 Q&As
  folded into Getting Started (Search, Weather). Only the icons actually used were imported (Search /
  WbSunny from the brief's ADD list are unused — omitted). 3 section titles + 10 Q&A strings ×11.

- **profile-avatar-top-spacing** (`ui/screens/ProfileScreen.kt`): added `Spacer(Modifier.height(14.dp))`
  before the avatar Box in the signed-in identity Column so the avatar isn't glued to the banner
  seam. Flat value (not tied to `bannerBottomPadding`), signed-out branch untouched.

**Localization:** `common_expand`/`common_collapse` (once, shared), `photo_suggestion_photo_count` /
`photo_suggestion_visit_count` plurals, `photo_suggestion_subtitle` placeholder change, 3 help
section titles + 10 help Q&A strings — all ×11 locales (values + de/es/fr/it/ja/nl/pl/pt/sv/zh-rCN).
Inserted via the BOM-script recipe; glyphs verified intact (CJK, accents, `&amp;`). `pl` plurals use
one/few/many/other; ja/zh use other-only.
