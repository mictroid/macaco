# Worklog — 2026-07-22

## Two Cowork briefs implemented — not yet committed/shipped

versionCode unchanged at 76 (1.7). Both briefs verified against live source before implementing,
then moved to `docs/DONE/`. `:app:compileDebugKotlin` passes clean.

### `code-brief-billing-library-9-upgrade.md` — Play Billing Library 8.0.0+ compliance

Play Console flagged Macaco 2026-07-22: apps must use Google Play Billing Library 8.0.0+ by
**Aug 31, 2026** or updates are rejected. Macaco goes through RevenueCat, which bundles Billing
Library internally, so the fix is a dependency bump only.

- Bumped `revenueCat = "8.10.0"` → `"9.29.1"` in `gradle/libs.versions.toml` (RevenueCat Purchases
  SDK first bundled Billing Library 8.x at v9.0.0; stayed on 9.x to avoid the unverified 10.x major).
- Verified `BillingManager.kt` uses no `.copy()`/destructuring on RevenueCat model types (the two
  breaking changes in the 8x→9x migration) — none present, no code changes needed. Confirmed by the
  clean compile resolving the 9.29.1 dependency.
- **Files:** `gradle/libs.versions.toml` only.
- **Deviations from brief:** none.
- **Open action for Michael (not code):** confirm the Lifetime product is set **non-consumable** in
  the RevenueCat dashboard before shipping — Billing Library 8 removed querying consumed one-time
  products, so a mis-set Lifetime would silently break future restores.

### `code-brief-drive-download-race.md` — serialize overlapping Drive download passes

Large first-time syncs (200+ photos, no local cache) could silently fail to load most photos while
the banner undercounts failures: the Firestore entries listener emits more than once during initial
load and each emission spawned an independent `downloadMissing()` pass with its own semaphore,
pushing aggregate concurrent Drive requests past the limit and tripping `403 userRateLimitExceeded`.

- Added `driveDownloadMutex = Mutex()` next to `driveUploadMutex`; wrapped `downloadMissing()`'s
  body in `driveDownloadMutex.withLock { }` (both `return@withContext` → `return@withLock`). A second
  overlapping call now waits and finds most work already in `cachedPhotoUris`. No new imports.
- **Files:** `data/sync/DrivePhotoSync.kt` only.
- **Deviations from brief:** none.
- **Does NOT fix:** genuine 403 retry/backoff on truly enormous libraries (separate follow-up).

### Shipped as vc77 (1.7) — DISPATCHED to Play Production

- Implementation committed `361fb14`; vc bump + release notes `dfa33d5`.
- `origin/master == HEAD == dfa33d5` verified before dispatch.
- Dispatched **2026-07-22 18:10:31 +0200**, WIF run **`29936801318`** ✅ **CONFIRMED SUCCESS**
  (10m22s) (https://github.com/mictroid/macaco/actions/runs/29936801318). **✅ CONFIRMED LIVE in Play
  Production** — cleared Google review 2026-07-22.
- **Lifetime non-consumable confirmed** in the RevenueCat dashboard before dispatch
  (`macaco_lifetime`, Play Store, Product Type: Non-consumable) — the billing brief's pre-ship gate.
- Release notes refreshed for the batch (320 codepoints, under the 500 limit).
- Not verified on-device (billing purchase/restore can't be tested on a sideloaded debug APK; needs
  a real Play-track build). Worth a Monthly/Annual/Lifetime + Restore smoke test on the Production
  build once live.
- Meets the Play Billing Library 8.0.0+ compliance deadline (2026-08-31).
