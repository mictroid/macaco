# Macaco worklog — 2026-06-29

## vc44 dispatched — 3-change batch (export fix + map-v11 + icon revert)

Shipped to Play closed testing via the WIF workflow.

- **versionCode:** 43 → 44 (versionName 1.6 unchanged)
- **HEAD:** `66f6200` — origin == HEAD verified before dispatch
- **Run ID:** 28351588403 — https://github.com/mictroid/macaco/actions/runs/28351588403
- **Dispatched:** 2026-06-29 07:53:16 +0200 (local dispatch time; `gh run list` shows run start)
- **Status:** ⏳ dispatched, NOT yet confirmed — check `gh run list` (~6 min build).

### Batch contents (since vc43)
- **compact-export-fix** (`40af7a9`) — `JournalBackup.compressToBytes`: split the `openInputStream`
  null-guard from the `inJustDecodeBounds` bounds-decode so compact backups stop dropping every photo
  (`photoUris:[]` bug). Verified vs live source; no deviations.
- **map-camera-v10 + v11** (`b7730f7`, `d5638d7`) — density-correct Mercator zoom + `minZoomPreference=1f`
  so the map frames ~2 pins best-effort (v10/v11 diagnosed on-device via Logcat — `move()` silently
  clamps to SDK min zoom). **Still owes in-the-wild visual verify on the A53.**
- **icon-revert** (`a2203bb`) — launcher monkey back to original (undo vc42's scale-1.15 enlargement).

### Release notes (refreshed for vc44)
- Fixed compact backups so they now include all your photos
- Improved the travel map so it frames your entry pins more reliably
- Restored the original app icon
- Bug fixes and polish

### On-device verification owed after vc44 publishes
- **map-camera-v11** (the key one — 11th attempt; verify it frames pins acceptably in the wild)
- compact backup export includes all photos (Settings → Export → Compact)
- home-screen icon back to original size

---

## compact-export-fix implemented (held for next batch, NOT shipped)

Cowork brief `code-brief-compact-export-fix.md` implemented and moved to `docs/DONE/`.

- **Commit:** `40af7a9` — `fix(backup): compact export no longer skips every photo`
- **File:** `app/src/main/java/com/houseofmmminq/macaco/data/sync/JournalBackup.kt`
- **Build:** `compileDebugKotlin` ✅ green (15s)
- **Status:** committed locally, NOT pushed — batched with the held map-v10/v11 + icon-revert commits.

### What it fixes
`compressToBytes` (compact-export path) opened the bounds-read stream as:

```kotlin
resolver.openInputStream(uri)?.use {
    BitmapFactory.decodeStream(it, null, boundsOpts)
} ?: return null
```

`?.use { block }` evaluates to the block's result. With `inJustDecodeBounds = true`,
`decodeStream` **always returns null by design** (reads dimensions only, no allocation), so the whole
expression was null and `?: return null` fired on **every** photo — regardless of whether the URI was
readable. Result: compact backups shipped with `photoUris: []` and no `photos/` zip entries, and
`ExportResult.photosSkipped` counted every photo. Now the guard checks the stream itself:

```kotlin
val streamForBounds = resolver.openInputStream(uri) ?: return null
streamForBounds.use {
    BitmapFactory.decodeStream(it, null, boundsOpts)
}
```

### Verification (per cowork-brief-workflow)
- Brief cited correct package (`com.houseofmmminq.macaco`) and real path (`data/sync/`).
- "BEFORE" snippet matched live source at lines 119–121 exactly.
- No new imports: `resolver.openInputStream`, `BitmapFactory.decodeStream` (fully-qualified) already
  in use. No deviations from the brief.

### On-device verification owed
Settings → Export Backup → Compact → unzip → confirm `photos/<uuid>_0.jpg` present and `backup.json`
entries have non-empty `photoUris`; snackbar `photosSkipped` should be 0 for readable media.
