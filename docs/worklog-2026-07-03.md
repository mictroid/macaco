# Macaco worklog — 2026-07-03

## 8 landscape/UI briefs implemented (post-vc52), NOT yet shipped

New Cowork batch of 10 briefs arrived. The 8 low-risk landscape/UI ones implemented together, each
verified vs live source first; `assembleDebug` **SUCCESSFUL** (1m44s); all 8 → `docs/DONE/`. Still on
vc52 on Play — next ship = vc53. (The 2 large/coupled video briefs — `video-entries`,
`reel-video-frames` — left pending in `docs/` for a separate decision: they add 3 Gradle deps
(transcoder + media3 ×2), a new `VideoTranscoder.kt`, ExoPlayer playback, Drive sync + backup changes,
manifest permissions, 15 strings ×11 locales, and the brief leaves the `DrivePhotoSync` video functions
as `// ... same pattern ...` stubs. `reel-video-frames` depends on `VideoTranscoder`.)

- **datepicker-landscape** (`NewEditEntryScreen.kt`) — `rememberDatePickerState` now opens in
  `DisplayMode.Input` when `screenHeightDp < 480` so the M3 calendar grid doesn't overflow in landscape.
  Added `import androidx.compose.material3.DisplayMode`. `LocalConfiguration` already imported.
- **drawer-landscape-restore** (`JournalListScreen.kt`) — reverses drawer-landscape-v2: removed the
  `if (!drawerIsLandscape)` guard so Share App + Rate Us show in landscape again; compacted the 3
  landscape spacers 8→4dp (top spacer + before-divider + trailing conditional).
- **journal-landscape-inset-scroll** (`JournalListScreen.kt`) — content Column gains
  `.windowInsetsPadding(WindowInsets.navigationBars.only(WindowInsetsSides.End))` (× button/tag chips
  clear the right-edge nav bar in landscape); `OnThisDayBanner` moved from the outer Column into the
  LazyColumn as the first `item` so the whole page scrolls as one unit. Added 4 layout imports
  (`WindowInsetsSides`, `navigationBars`, `only`, `windowInsetsPadding`; `WindowInsets` already present).
- **language-dialog-scroll** (`SettingsScreen.kt`) — language-picker Column gains
  `Modifier.verticalScroll(rememberScrollState())` so all 12 languages are reachable in landscape.
  `rememberScrollState`/`verticalScroll` already imported.
- **map-arrows-v15** (`MapScreen.kt`) — one directional arrow per off-screen pin (was one chevron per
  side): replaced the 4 `offScreen*Lng/Lat` vars with `offScreenWestPins`/`offScreenEastPins` `List<LatLng>`,
  tracker filters ALL pins per edge, rendering is two `forEach` loops using `ArrowUpward` rotated via
  `graphicsLayer { rotationZ = bearing }` (`atan2` bearing from screen-centre). *Deviations:* added
  `import kotlin.math.atan2` and `import androidx.compose.ui.graphics.graphicsLayer` (brief said add
  atan2/ArrowUpward but graphicsLayer was genuinely absent); removed now-unused ChevronLeft/Right imports.
- **primary-container-tint** (`JournalListScreen.kt`) — `OnThisDayBanner` card + `TripHeader` row
  backgrounds `primaryContainer` → `primary.copy(alpha = 0.12f)` so light mode reads as the chosen theme
  colour (unrelated `primaryContainer` uses left untouched).
- **profile-header-adventure-style** (`ProfileScreen.kt`) — profile header matches the adventure header:
  portrait adds a 44dp natural-colour icon (top padding 18→8dp, macaco 24→20sp/ls 5); landscape icon
  24→20dp natural colour (macaco 16→14sp/ls 3), gold tint removed. *Deviation:* removed the now-unused
  `import androidx.compose.ui.graphics.ColorFilter`.
- **profile-landscape-nav-inset** (`ProfileScreen.kt`) — right pane Box gains
  `.windowInsetsPadding(WindowInsets.navigationBars.only(WindowInsetsSides.End))` before the 20dp padding.
  *Deviation:* brief claimed `WindowInsets`/`WindowInsetsSides` already imported — only `WindowInsets`
  was; added `WindowInsetsSides`, `navigationBars`, `only`, `windowInsetsPadding`.

## On-device verify — S8+ (Android 9, Play vc52) — CONFIRMED

New second test device (Galaxy S8+ SM-G955F) connected via ADB alongside the A53; running the Play
closed-testing vc52 build. User confirmed on-device: **vc52 landscape** (drawer Sign Out visible; header
icon above "macaco"; profile stats card in right pane; Settings stacked/tighter), **vc50 localization**
(strings translate on language switch), **vc51 compat-theme-crash** (no crash on language change /
rotation), **vc51 map-arrow-contrast** (chevrons visible in light mode). Remaining owed: Ship-1
data-integrity, Ship-2 auth/billing, Ship-3 camera-process-death / backup-lifecycle / branded snackbar.

## vc52 / 1.6 SHIPPED 2026-07-03 — 4 landscape/UI briefs — WIF run `28661833076` ✅ SUCCESS — PUBLISHED

Bump vc51→52 + refreshed `default.txt` commit `1b7a0c6`; batch commit `bdfd0d0`. Pushed;
**origin == HEAD (`1b7a0c6`) verified before dispatch**. Dispatched **14:53:15 +0200** (local) —
https://github.com/mictroid/macaco/actions/runs/28661833076. **Dispatched, NOT yet confirmed** (~6 min).
All 4 verified vs live source first; `assembleDebug` **SUCCESSFUL**; all → `docs/DONE/`. (vc51 confirmed
SUCCESS before this batch — published.)

- **drawer-landscape-v2** (`JournalListScreen.kt`) — v1's spacer fix wasn't enough (content ~456dp >
  A53's ~443dp landscape height). Hide Share App + Rate Us drawer items when `drawerIsLandscape` (both
  still reachable from Help & About) so Settings/Dark Mode/Help/Sign Out always fit. Portrait unchanged.
- **landscape-header-icon** (`JournalListScreen.kt`, `ProfileScreen.kt`, `SettingsScreen.kt`) — the 3
  landscape compact headers now stack the monkey icon ABOVE "macaco" (Row→Column); Settings header
  became a Box (back button `CenterStart`, brand Column `Center`) to match the Profile pattern.
- **profile-landscape-v4** (`ProfileScreen.kt`) — moved the full stats Card (Memories|Trips|Locations|
  Photos) from the left pane to the right pane; dropped the redundant standalone "N Trips" row v3 added.
  *Deviation:* removed the now-unused `import ...icons.outlined.Flight`.
- **settings-stacked** (`SettingsScreen.kt`) — reverted the landscape side-by-side Theme Color /
  Background Image `Row` to a single stacked column for all orientations (content Column already scrolls);
  tightened spacing (outer Column `spacedBy` 8→4dp, `vertical` padding 8→4dp, removed the redundant
  `Spacer(4.dp)` blocks before section headers). `val isLandscape` kept (card lambdas still read it).

## vc51 / 1.6 SHIPPED 2026-07-03 — 2 post-vc50 briefs — WIF run `28660481187` ✅ SUCCESS

Bump vc50→51 + refreshed `default.txt` commit `ef60a07`; CONFIRMED SUCCESS, published to closed testing.
Original DISPATCHED entry — WIF run `28660481187`

Bump vc50→51 + refreshed `default.txt` commit `ef60a07`. Pushed; **origin == HEAD (`ef60a07`) verified
before dispatch**. Dispatched **14:26:51 +0200** (local) —
https://github.com/mictroid/macaco/actions/runs/28660481187. **Dispatched, NOT yet confirmed** (~6 min;
`gh run list` to check). Batch = compat-theme-crash + map-arrow-contrast (both detailed below).

Two Cowork briefs arrived after vc50 dispatched. Both verified vs live source first; `compileDebugKotlin`
**SUCCESSFUL** (Kotlin-only, no resources). Both → `docs/DONE/`.

- **compat-theme-crash** (commit `acf1644`, `MainActivity.kt`) — fixes the vc48-introduced
  `IllegalStateException: You need to use a Theme.AppCompat theme` crash on Activity recreation (locale
  change / rotation). The vc48 language-flash fix had guarded `installSplashScreen()` with
  `if (savedInstanceState == null)`, which also skipped its `Theme.Macaco.Splash` → `Theme.MyApplication`
  (AppCompat) theme swap on recreation, leaving a non-AppCompat theme active so
  `AppCompatActivity.super.onCreate()` threw. Now called unconditionally; the vc48 windowBackground fix
  already makes the teal→teal swap flash-free, so the guard's original motivation is gone. No deviations.
- **map-arrow-contrast** (commit `75c3334`, `MapScreen.kt`) — the ◀▶ off-screen-pin chevrons used pale
  `primaryContainer.copy(alpha = 0.85f)` (near-invisible on light-blue ocean tiles in light mode). Both
  west + east chevrons now use opaque `primary` background + `onPrimary` icon tint + `shadow(4.dp,
  CircleShape)` before `clip` so the circle pops off any tile. East chevron's nav-bar End inset unchanged.
  *Deviation:* added `import androidx.compose.ui.draw.shadow` — brief wrongly claimed it was already
  present (only `androidx.compose.ui.draw.clip` was).

## vc49-QA sweep — Ship 1 of 3 (HIGH data-integrity trio) implemented, NOT yet shipped

Full-app code QA at vc49 (`docs/qa-code-review-2026-07-03.md`, Fable 5) produced 9 briefs. Grouped
into 3 ships by risk. **Ship 1 = the three HIGH-severity data-integrity fixes**, verified vs live
source first, built together (`assembleDebug` **SUCCESSFUL**, 10m37s). All → `docs/DONE/`. Not yet
version-bumped/dispatched (still vc49 on Play).

- **drive-ids-alignment** (QA H1) — `NewEditEntryScreen.kt`, `EntryDetailScreen.kt`. Keeps the
  positional `driveFileIds` list aligned with `photoUris` through every photo edit. Added a parallel
  `driveIds` rememberSaveable state (padded to photoUris length with `""`), mirrored through
  picker-add, camera-add, remove, drag-reorder, and passed as `driveFileIds` on Save (was
  `existingEntry?.driveFileIds` = original order). Detail screen: new `paddedDriveIds()` helper;
  `withCover`/`withRemoved`/`withSwapped` now pad-then-mutate unconditionally (previously skipped the
  driveIds mutation when the list was shorter, desyncing pending-upload photos).
  *Deviation:* preserved the `sessionAdded` file-cleanup block in the remove handler that the brief's
  BEFORE snippet omitted; dropped `.distinct()` on adds per the brief (persistToGallery always makes a
  unique copy, and distinct() would break the parallel-list invariant).
- **save-entry-upload-race** (QA H2) — `JournalViewModel.kt`. Background Drive-upload completion no
  longer re-saves the stale captured entry (which reverted mid-upload edits). `saveEntry` and
  `syncPhotosToGoogleDrive` now merge only `driveFileIds` into the *live* entry looked up by id, and
  skip the write if `photoUris` changed since upload start (indices no longer match; next save
  re-uploads). No deviations.
- **photo-exif-rotation** (QA H3) — `ImageStorage.kt`, `JournalBackup.kt`, `libs.versions.toml`,
  `app/build.gradle.kts`. Camera photos no longer render sideways: re-encode paths now read the EXIF
  Orientation tag and bake the rotation into the bitmap before compressing (BitmapFactory ignores the
  tag and Bitmap.compress writes none). Added `androidx.exifinterface:1.3.7`; new
  `ImageStorage.exifOrientation` (private) + `applyExifOrientation` (`internal` so `JournalBackup`
  reuses it) wired into `compressForStorage` and compact-export `compressToBytes`. No deviations.

## vc49-QA sweep — Ship 2 of 3 (auth & billing) implemented, NOT yet shipped

Second batch, verified vs live source first, `assembleDebug` **SUCCESSFUL**. Both → `docs/DONE/`.
Still on top of Ship 1 locally; nothing pushed/dispatched yet.

- **billing-signout-reset** (QA M2) — `BillingManager.kt`. Previous user's premium no longer leaks to
  the next account. Auth-collect now handles the signed-out branch: `applyEntitlement(null)` immediately
  (paywall correct instantly) then `Purchases.logOutWith` to detach the RevenueCat identity (ignore the
  already-anonymous error). `restore()` routed through `applyEntitlement` so plan-id/manage-subscription
  state stays fresh. Added `import com.revenuecat.purchases.logOutWith`. No deviations.
- **delete-account-reauth** (QA M5) — `AuthRepository.kt`, `FirebaseAuthRepository.kt`,
  `MockAuthRepository.kt`, `JournalViewModel.kt`, `NavGraph.kt`, `ProfileScreen.kt`, `strings.xml` ×11.
  Re-authenticate FIRST, then wipe data, then delete the account — so data is never lost to a
  `FirebaseAuthRecentLoginRequiredException`. Google users: silent `GoogleSignIn.silentSignIn()` re-auth;
  email users: new password field in the delete dialog (shown only for `AuthProvider.Email`, gated so
  confirm needs a non-blank password). `deleteAccount(password: String? = null)` plumbed through the
  interface → VM → NavGraph → screen; new private `ReauthException`; localized error mapping. 4 new
  string keys ×11 (`profile_delete_password_label`/`_required`/`_wrong_password`/`_reauth_google_failed`).
  *Deviation:* added `import com.houseofmmminq.macaco.R` to `FirebaseAuthRepository` (brief listed it but
  it was genuinely absent); `FirebaseConfig` same-package so no import (as brief said).

## vc49-QA sweep — Ship 3 of 3 (UX + localization) implemented, NOT yet shipped

Final batch — 4 briefs, each verified vs live source first and committed separately (one build per
brief, all `assembleDebug` **SUCCESSFUL**). All → `docs/DONE/`. Still nothing pushed/dispatched (all
of Ship 1+2+3 sits local on top of vc49).

- **qa-polish** (QA M4, commit `a61daeb`) — `EntryDetailScreen.kt`, `NewEditEntryScreen.kt`,
  `ProfileScreen.kt`, `JournalViewModel.kt`, `AdventureReelEncoder.kt`. Four independent fixes:
  (1) `BackHandler` closes the full-screen gallery overlay before popping the screen;
  (2) camera `pendingCameraUri` → `rememberSaveable` string in both camera flows so a process-death
  kill no longer drops the captured photo;
  (3) reel resolves photos per-index (Drive-cache fallback, was all-or-nothing `ifEmpty`),
  `check(framesRendered > 0)` fails a zero-frame encode instead of shipping a broken mp4, and
  `drawBranding` uses the pre-allocated pill/text/logo Paint fields instead of re-allocating per frame;
  (4) `shareEntry` resolves Drive-cached photos per index so sharing works on a reinstalled device.
  *Deviations:* added `import androidx.activity.compose.BackHandler`; added `rememberSaveable` import to
  ProfileScreen (brief implied present); `file_paths.xml` unchanged — `<cache-path path="."/>` already
  exposes `drive_photos/`.
- **backup-lifecycle** (QA M1, commit `76e1c5c`) — `JournalViewModel.kt`, `SettingsScreen.kt`. Backup
  export/import no longer cancelled mid-write when leaving Settings. VM owns `BackupBusy` StateFlow +
  `exportBackupInBackground`/`importBackupInBackground` running in `viewModelScope` (suspend
  `exportBackup`/`importBackup` kept as internals); SettingsScreen collects the hoisted state, a local
  `pickerPending` covers only the SAF picker window, `BackHandler` swallows back while busy, launchers
  use `applicationContext` toasts. *Deviation:* removed now-unused `rememberCoroutineScope` import,
  added `BackHandler` import.
- **branded-snackbars** (commit `9c8c6f5`) — `JournalViewModel.kt`, new
  `ui/components/MacacoSnackbar.kt`, `MainActivity.kt`, `JournalListScreen.kt`, `strings.xml` ×11.
  `startReel` fold maps `CancellationException` → `Idle` (cancel is silent, kills the raw
  "StandaloneCoroutine was cancelled" snackbar), our own `IllegalStateException` guard messages pass
  through, everything else → localized `reel_error_generic` (new key ×11). New branded `MacacoSnackbar`
  (teal `primary` container, monkey mark, `secondaryContainer` action — token-only so all 7 themes keep
  contrast) wired as the content slot of both `SnackbarHost`s.
- **hardcoded-strings** (QA M3, commit `f9041dd`) — `MapScreen.kt`, `ProfileScreen.kt`,
  `JournalListScreen.kt`, `PurchaseScreen.kt`, `EntryDetailScreen.kt`, `NewEditEntryScreen.kt`,
  `ReminderWorker.kt`, `ReminderScheduler.kt`, `NotificationCopy.kt`, `strings.xml` ×11. 27 new keys
  added & translated across all 11 locales (map title/mapped-count/empty-state/marker snippets, Member
  since, Not signed in, paywall footer, share chooser/caption/credit, Trip field, notification
  actions/channel, 4 reminder-copy variants). `buildNotificationCopy` now takes a `Context` and resolves
  resources; ReminderWorker passes `ctx`. Brand slogan "Roam Freely. Forget Nothing." kept identical per
  locale. *Deviations:* added `import ...macaco.R` to ReminderScheduler; landscape map count now uses the
  same "N of M locations mapped" key as portrait (brief maps both to `map_locations_mapped`); EN
  apostrophes escaped as `\'`.

### Ship status
All 9 QA briefs (Ships 1+2+3) implemented and committed. **SHIPPED as vc50** in one dispatch.

## vc50 / 1.6 SHIPPED 2026-07-03 — vc49-QA sweep (all 3 ships, 9 briefs) — WIF run `28657102992` ✅ SUCCESS

Bump vc49→50 + refreshed `default.txt` commit `a501a57`. Pushed; **origin == HEAD (`a501a57`) verified
before dispatch**. Dispatched **13:17:56 +0200** (local) via `gh workflow run release.yml` —
https://github.com/mictroid/macaco/actions/runs/28657102992. **CONFIRMED SUCCESS** (6m25s, 11:17:57→
11:24:22 UTC) — **PUBLISHED to closed testing.** Clean first dispatch (no stale-token retry). Batch = the full 9-brief vc49-QA sweep: Ship 1 (drive-ids-alignment,
save-entry-upload-race, photo-exif-rotation), Ship 2 (billing-signout-reset, delete-account-reauth),
Ship 3 (qa-polish, backup-lifecycle, branded-snackbars, hardcoded-strings ×11 locales).

**On-device verify owed once vc50 publishes** (A53, reinstall from Play — see note): Ship 1 (edit-reorder
multi-photo entry survives reinstall; camera portrait upright in-app + Drive/backup); Ship 2 (premium→
signout→non-premium paywall; delete email wrong/right password + Google happy/revoked); Ship 3 (gallery
back closes overlay; camera-after-process-death photo kept; backup survives leaving Settings; branded
snackbar look; non-English strings render in all 11 locales; reminder notification copy localized).

### On-device verify owed (Ship 2)
Two Google accounts + one email/password account: sign in premium → sign out → sign in non-premium →
PurchaseScreen must appear. Delete flow: email wrong-password (data untouched), email correct, Google
happy path, Google with revoked GMS session (reauth-failed message, data untouched).

Verification still to do on device before/after shipping: edit-reorder a multi-photo entry (pairing
survives reinstall), and confirm a fresh camera portrait renders upright in-app + in Drive/backup.
