# Macaco worklog — 2026-07-06

## vc62 / 1.6 DISPATCHED — brand-header consistency batch — WIF run `28783329685`

Dispatched **2026-07-06 11:57:48 +02:00** (09:57:50 UTC) → run
[`28783329685`](https://github.com/mictroid/macaco/actions/runs/28783329685). versionCode 61→62,
versionName 1.6. Bump+notes commit `fc44bfe`; **origin == HEAD verified** (`fc44bfe`) before dispatch.
Implementation commit `d9ac9d4`. `compileDebugKotlin` clean. **Dispatched, NOT confirmed** — check
`gh run list` (~6 min). **Not yet on-device verified** (user waived verification for this batch).

Batch of 3 Cowork briefs, all verified vs live source first:

1. **content-width-landscape-v2** (`ContentWidth.kt`) — `macacoContentGutter()` drops the
   `smallestScreenWidthDp >= 600` tablet-only gate; now **any** landscape (phone incl. A53, or
   tablet) skips the 840dp content cap. The separate `WindowInsets.safeDrawing` nav-bar/cutout
   protection in JournalListScreen/HelpAboutScreen is untouched. BEFORE matched live exactly.

2. **macaco-brand-header-consistency** — new shared `ui/components/MacacoBrandBlock.kt` (fixed 48dp
   icon; wordmark 13sp/letterSpacing-3 portrait, 16sp/letterSpacing-4 landscape, `FontWeight.Light`,
   `SplashGoldBright`; collapsed = icon-only). Replaced the hand-rolled icon+wordmark blocks in all
   five headers so sizing can't drift again: `JournalListScreen.kt` (reference — pure refactor,
   collapsed/landscape/portrait), `MapScreen.kt`, `ProfileScreen.kt` (was 36dp/18sp → shared; added
   an `isLandscape` variant it lacked), `SettingsScreen.kt` (landscape was 22dp, portrait 44dp/20sp),
   `HelpAboutScreen.kt` (was 64dp icon everywhere; portrait 22sp/6sp). Tablet coverage is free —
   every screen's `isLandscape` is `screenHeightDp < 480`, so tablets always take the expanded branch.
   - **Deviation:** the brief's import `androidx.compose.ui.draw.offset` is wrong (no such symbol for
     the dp `Modifier.offset`); corrected to `androidx.compose.foundation.layout.offset`, matching
     every live screen. Recorded as the recurring "brief wrong import" class.

3. **profile-adventures-collapsible-header** (depends on #2) — `ProfileScreen.kt`: hoisted the
   throwaway `rememberScrollState()` into `profileScrollState`, derived `collapsed` via
   `derivedStateOf { value > 24 }` (same threshold as Journal/Help), passed to `MacacoBrandBlock`;
   banner `Box` got `.animateContentSize()`. `MapScreen.kt`: one-way `hasMovedMap` latch driven by
   `LaunchedEffect(cameraPositionState.isMoving)`, collapses the header on first pan/zoom; unified
   the two `MacacoBrandBlock` call sites (from brief #2) into one with `landscapeTrailing`/
   `portraitTrailing` slots.
   - **Deviation / note:** MapScreen's #2 + #3 edits were applied together as the single final
     unified call site (#3 rewrites #2's output anyway). Preserved the live portrait count styling
     (`fontSize = 12.sp, fontFamily = MacacoFontFamily`) that the briefs' BEFORE snippets omitted, to
     avoid a styling regression.

No new strings; no localization. All three briefs → `docs/DONE/`.
