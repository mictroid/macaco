# Worklog — 2026-07-19

## Widgets + Drive-dedupe batch — committed `eb1fe93`, NOT pushed, NOT shipped (→ vc74)

Resumed the in-progress widgets batch (sessions 1–2 uncommitted in the working tree) and closed it
out: implemented the two remaining Cowork briefs, fixed the long-standing blank-widget-photo bug at
its real root cause, fixed an ANR the photo fix surfaced, and added a user-requested UI change.
Committed the whole batch locally as `eb1fe93` (50 files). **Not pushed** — pushing deploys the
updated legal pages via GitHub Pages, held pending user OK. Ships as **vc74** when the user says
(vc73 confirmed live + not crashing on Play this session).

### Widget photos — REAL root cause found + fixed (verified on S8)

Sessions 1–2 misdiagnosed this as a Samsung One UI `file://` quirk. The actual bug: both widget
decoders (`OnThisDayWidgetProvider.decodeThumbnail`, `RecentEntriesWidgetService.decodeThumbnail`)
ran the `inJustDecodeBounds` bounds pass as:

```kotlin
WidgetPhotos.openStream(context, uri)?.use { BitmapFactory.decodeStream(it, null, boundsOpts) } ?: return null
```

`BitmapFactory.decodeStream` **returns null by design** when `inJustDecodeBounds = true` (it only
fills `boundsOpts`), so `?: return null` misread that expected-null as a failure and aborted the
function *before the real decode ever ran*. Confirmed on-device via temporary logcat: `streamNull=false`
(stream opened fine, cache file valid) but the `decode bounds=` log never printed → `bitmap=nullxnull`.

Fix: guard the **stream**, not the decode result —
`(WidgetPhotos.openStream(context, uri) ?: return null).use { BitmapFactory.decodeStream(it, null, boundsOpts) }`
then `if (boundsOpts.outWidth <= 0 || boundsOpts.outHeight <= 0) return null`. Applied to both decoders.
On-device (S8 SM-G955F): On This Day hero shows the Sagrada Família photo; all four Recent-entries rows
show real thumbnails. **General rule: never `?: return null` on the result of an `inJustDecodeBounds`
`decodeStream` — that result is legitimately null.**

The `WidgetPhotos.openStream` `file://`-via-`FileInputStream` split (from session 2) is kept — it's a
correct, separate fix — but was necessary-not-sufficient; the null-guard bug above was the real blocker.

### code-brief-widget-photo-loading-fix

Wired `DrivePhotoSync.ensurePhotosCached` into the widgets so they self-populate the Drive photo cache
without the app having run (OnThisDay `fetchHighlight`, RecentEntries `onDataSetChanged` via
`runBlocking` on its binder thread). Removed the Fix-3 debug logging. Note: this brief's cache-population
theory was NOT the actual bug (the decode logic bug was), but the cache wiring is correct and retained.

### ANR fix (introduced by the photo brief, then fixed)

The photo brief's Fix 1 had `fetchHighlight` call `ensurePhotosCached(entries)` for the **whole
library**, which blocked the widget-update `goAsync` broadcast past its ANR window on a cold cache —
reproduced twice as `am_anr … OnThisDayWidgetProvider`. Fixed by scoping the blocking download to only
the chosen highlight entry: `ensurePhotosCached(listOf(highlight))` (1–3 files). Re-tested with the
`drive_photos` cache cleared: no ANR, photos render. RecentEntries keeps its `runBlocking` (binder
thread, more lenient than a broadcast; rendered 8 thumbnails without ANR).

### code-brief-drive-dedupe-and-doc-updates

`DrivePhotoSync`:
- **Fix 1 — Mutex:** new `driveUploadMutex` serializes the find-or-create-folder + upload loops in
  `uploadEntryPhotos`/`uploadEntryVideos`, killing the same-process race that created duplicate
  "Macaco" folders / duplicate uploads (auto-upload-on-save racing the manual Sync Now).
- **Fix 2 — idempotency:** `uploadPhoto`/`uploadVideo` now take `entryId` + `index` and name files
  deterministically (`macaco_<entryId>_<i>.jpg/.mp4`); a new `findExistingFile` queries Drive for that
  name before creating, so a retry from any device/process reuses the existing file instead of
  duplicating — a cross-device guarantee the in-process mutex alone can't give.
- **Fix 3 — self-heal:** `getOrCreateDriveFolder` now orders folders by `createdTime` and picks the
  **oldest** when duplicates already exist (logs a warning), so future uploads converge on one folder.
  Does not auto-delete extras (too risky) — flagged as manual owner cleanup.

Docs (Fix 4/5): 3 FAQ strings (`help_faq_a_photos`, `help_faq_a_backup`, `help_faq_drive_connect_a`)
updated to say "photos and videos" across all 11 locales; `privacy-policy.html` + `terms-of-service.html`
updated for video backup across every embedded language block, with "Last updated" → 19 July 2026.
The brief's "remove stale Apple Sign-In" step was a **no-op** — the live HTML already had no Apple
mentions (verified against source, per the verify-first rule).

### Settings — collapsible widgets card (user request)

The "Home-screen widgets" section (header + 4 full pin rows) took a lot of vertical space. Replaced it
with `ExpandableWidgetsCard` — a single tappable card (Widgets icon + title + rotating chevron) that
expands to reveal the 4 pin rows, collapsed by default. Reuses existing strings (`settings_widgets`,
`settings_widget_add_value`); dropped the now-redundant section header.

### Session-1/2 pieces folded into this commit (context)

4 widgets total (On This Day redesign with header + gold date, Recent Entries collection widget with
row deep-link via `MainActivity.ACTION_OPEN_ENTRY`, Travel Stats, Quick Add), distinct picker labels
(`widget_label_*` ×11), raster previews (`res/drawable-nodpi/widget_preview_*.png`), `previewLayout`
for API 31+, `WidgetIntents.openAppPendingIntent` (resume instead of cold-start), monkey mark
`ic_macaco_widget_mark.xml`, and the RemoteViews bare-`<View>`→`FrameLayout` scrim fix.

### Briefs archived

All six moved to `docs/DONE/`: `additional-widgets`, `widget-exported-fix`, `widget-faq-update`,
`help-about-trial-copy-fix`, `widget-photo-loading-fix`, `drive-dedupe-and-doc-updates`.

### State

`assembleDebug` green; debug APK installed + verified on S8; working tree clean of debug scaffolding
(`.idea/claudeCodeEditorTabs.xml` intentionally left untracked). Commit `eb1fe93` local only —
**not pushed, not dispatched.**

### Open owner follow-up

- Delete pre-existing duplicate "Macaco" Drive folders by hand (code converges on the oldest going
  forward but doesn't auto-delete the extras).
- Push (deploys the updated privacy/terms pages) + ship vc74 when ready.
