# Worklog — 2026-07-20

## Two follow-up bugfix briefs implemented on branches, PRs open (not shipped)

Automated scheduled-task run. versionCode unchanged at 74 (1.6) — neither PR has been merged to
master or shipped; both are pending review + on-device verification.

### `feature/drive-dedupe-position-collision` — commit `ad7f738` — [PR #1](https://github.com/mictroid/macaco/pull/1)

Implements `docs/DONE/code-brief-drive-dedupe-position-collision.md`. Fixes the vc74 Drive-dedupe
fix silently reassigning a still-present photo/video's Drive file to a different one after a
delete-then-add (or reorder/set-cover) shifts array positions, because the idempotent-upload
filename lookup keyed on position alone with no check against Drive ids already claimed elsewhere
in the same entry. `uploadPhoto`/`uploadVideo` now take the in-progress `result` list and skip
reuse of any id already claimed by another index, uploading a fresh file instead.

- **File:** `DrivePhotoSync.kt` only.
- **Deviations from brief:** none — implemented exactly as specified.
- **Verification:** `./gradlew compileDebugKotlin` passes. On-device repro (delete middle photo,
  add new photo, confirm distinct Drive file) **not yet run** — do before merge.

### `feature/photo-fallback-premium-lapse` — commit `6405d23` (+ `6d178c5` worklog) — [PR #2](https://github.com/mictroid/macaco/pull/2)

Implements `docs/DONE/code-brief-photo-fallback-premium-lapse.md`. `JournalViewModel`'s auto
`downloadMissingPhotos` call was gated behind `isPurchased == true`, so a lapsed premium/trial
account stopped restoring Drive-backed photos whose local URI doesn't resolve on-device (cross-device
entries, or a removed gallery copy) — even though those photos were legitimately uploaded while the
account was premium. Removed the gate on this read-only restore path (grants no new capability);
upload/connect paths (`saveEntry`, `syncPhotosToGoogleDrive`, `SettingsScreen`'s `DriveBackupCard`)
remain premium-gated, unchanged.

- **File:** `JournalViewModel.kt` only.
- **Deviations from brief:** none — implemented exactly as specified.
- **Verification:** `./gradlew compileDebugKotlin` passes. On-device confirmation (lapsed account
  still sees previously-synced photos; upload/connect still gated) **not yet run** — do before merge.

Both branches built independently off `master` (815d93e) — each PR touches only its own file, no
conflict expected on merge. Both briefs moved to `docs/DONE/`. `docs/worklog.md` rolling summary
updated in the same branch/commit as the photo-fallback change.
