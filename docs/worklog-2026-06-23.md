# Macaco worklog — 2026-06-23

3-brief batch implemented + committed to `master` (NOT yet bumped/pushed/published).

## vc36 SHIPPED — 3-brief batch #2 dispatched

Bumped versionCode 35→36 (versionName still 1.5). Refreshed `default.txt` (password reset / tablet
hero photo / map tablet zoom). Brief impl in `76e1d48`; bump + notes in `4f5f253`. Pushed
`5377106..4f5f253`; **origin == local HEAD (`4f5f253`) verified before dispatch.** WIF release.yml
dispatched separately → **run `28058165094`, dispatched 2026-06-23 23:25:30 local (+02:00)**.
https://github.com/mictroid/macaco/actions/runs/28058165094
Not watched (minimal-push); confirm later with `gh run list`. Not device-verified.

## 3-brief batch #2 (entry-detail hero / forgot-password / map-camera-v5) — impl detail

All three Cowork briefs verified vs live source first — every "BEFORE" snippet, signature, and
import claim matched live source (map brief's "already imported" claim for `LatLng`/`LatLngBounds`/
`CameraUpdate(Factory)` was correct this time; `LocalConfiguration` was NOT yet imported in
EntryDetail, exactly as that brief stated). `assembleDebug` BUILD SUCCESSFUL. Briefs moved to
`docs/DONE/`.

| Brief | File(s) | Notes / deviations |
|-------|---------|--------------------|
| entry-detail-image-height | EntryDetailScreen.kt | Added `LocalConfiguration` import; computed `heroHeight` = 52% screen height on `sw600dp+` tablets, else 260dp; applied to single-photo `JournalPhoto` and multi-photo collage `Row`. As-brief, no deviations. No strings. |
| forgot-password | AuthRepository.kt, FirebaseAuthRepository.kt, MockAuthRepository.kt, JournalViewModel.kt, LoginScreen.kt, strings.xml ×11 | New `sendPasswordResetEmail` on the interface + Firebase impl (hedged error mapping via existing `mapFailure`); ViewModel callback wrapper; "Forgot password?" TextButton (sign-in mode only) + `primaryContainer` success banner with hedged `login_forgot_sent` text; reset state cleared on toggle to create-account. 3 new keys ×11 locales via bulk-localize skill. **Deviation 1 (not in brief):** also implemented the method in `MockAuthRepository` — it implements `AuthRepository`, so the build failed until added (`= Result.success(Unit)`). **Deviation 2:** escaped the apostrophe in the French `login_forgot_enter_email` (`d\'abord`) — aapt rejected the raw apostrophe. |
| map-camera-v5 | MapScreen.kt | Replaced `newLatLngBounds` (needs a laid-out map; threw `IllegalStateException("Map size can't be 0")` on tablet) with layout-independent center+approx-zoom from the bounds span via `newLatLngZoom`; gated `cameraPositioned = true` on `move().isSuccess` so a failure can't strand the scrim (8s timeout still backstops). As-brief, no deviations. No strings. |

## 3-brief batch — commit `2bfd11a`

All three Cowork briefs verified vs live source first (no stale-clone drift this time — signatures,
line numbers, and "current code" snippets all matched). `assembleDebug` BUILD SUCCESSFUL; debug APK
produced. Briefs moved to `docs/DONE/`. Single commit `2bfd11a`.

| Brief | File(s) | Notes / deviations |
|-------|---------|--------------------|
| backup-import-zlib-fix | JournalBackup.kt (importFrom) | Two-phase import: **Phase 1** copies the SAF/Drive source to a local `tempDir/source.zip` (via the existing `CountingInputStream` for download progress); **Phase 2** opens that local file as `ZipInputStream`. Fixes `ZipException: ZLIB inflate error` — opening ZipInputStream directly over a chunked Drive-backed SAF stream desyncs the ZLIB decompressor when `closeEntry()` stalls mid-drain. Local file reads are contiguous → no stall. `srcZip.delete()` after extract; `tempDir` cleaned in `finally`. **Extra (not in brief):** removed the now-unused `java.io.BufferedInputStream` import (the new body uses `srcZip.inputStream().buffered(...)`). As-brief otherwise. |
| map-default-position-v2 | JournalViewModel.kt + MapScreen.kt | Camera was locking onto the **first** geocoded result instead of fitting all locations (regression from v1's scrim fix — the effect fired as soon as `geocodedLocations` became non-empty). Added `geocodingComplete: StateFlow<Boolean>` (reset false at start of `geocodeLocations`, true when the loop finishes incl. empty/all-fail); changed the camera `LaunchedEffect` key from `geocodedLocations` → `geocodingComplete` and added the guard. Null-Island filter + >30° outlier rejection + delay/retry move unchanged. As-brief. No strings. |
| profile-photo-account-switch | JournalViewModel.kt | One-liner: clear the local custom profile photo on sign-out so the next account doesn't inherit it. Added `if (user == null) preferencesManager.setProfilePhotoUri(null)` inside the existing UID-change block in the auth listener. `setProfilePhotoUri(null)` already removes the DataStore key (verified PreferencesManager:83). As-brief. No strings. |

## vc32 SHIPPED — bumped, notes refreshed, pushed, WIF dispatched
- Bumped versionCode 31→32 (versionName still 1.5, per convention). Refreshed `default.txt` What's
  new for this batch (backup ZLIB fix / map fits all locations / profile photo on account switch).
- Commit `2245c32` (bump + notes). Pushed `3659c46..2245c32` to master; verified `origin/master`
  == local HEAD (`2245c32`) BEFORE dispatching.
- WIF release.yml dispatched as a separate command after the push landed →
  **run `28004897407`, dispatched 2026-06-23 07:40:32 local (+02:00)**.
  https://github.com/mictroid/macaco/actions/runs/28004897407
  Did not watch (minimal-push convention); confirm later with `gh run list`.

**vc32 run `28004897407` confirmed SUCCESS** (5m57s, completed 05:46 UTC 2026-06-23). In closed testing.

## tablet-ui brief — commit `4d2f41d` (NOT yet bumped/pushed/published)

`code-brief-tablet-ui.md` implemented. All signatures verified vs live source first (clean match).
Four `sw600dp+` layout fixes via `LocalConfiguration.current.screenWidthDp >= 600`, no new deps.
`assembleDebug` BUILD SUCCESSFUL. Brief → `docs/DONE/`. Single commit `4d2f41d` on `master` (local-only).

| Fix | File | Notes |
|-----|------|-------|
| TagChips truncation | JournalListScreen.kt | Dropped `clipToBounds()` from the Row; added `overflow = TextOverflow.Ellipsis` + `widthIn(max=100.dp)` to chip Text. Fixes `#moun`/`#adv` hard-clip. |
| Entry list padding | JournalListScreen.kt | LazyColumn horizontal contentPadding 16→80dp on tablet. |
| EntryPhotoArea height | JournalListScreen.kt | Photo 120→200dp, no-photo placeholder 72→120dp on tablet. |
| New/Edit form padding | NewEditEntryScreen.kt | LazyColumn horizontal contentPadding 16→100dp on tablet. |

**Deviations:** added the `widthIn` + `LocalConfiguration` imports the brief flagged as maybe-missing
(both were); **removed the now-unused `clipToBounds` import** (Fix 1 was its only use). Both correct.
Not device-verified (no tablet here — best confirmed on the Tab A9+).

**Three NEW briefs arrived in `docs/` (untriaged):** `code-brief-backup-import-zlib-fix-v2.md`,
`code-brief-map-default-position-v3.md`, `code-brief-profile-google-photo-fallback.md`. The v2/v3
ones likely follow up on today's vc32 fixes — verify vs live before implementing.

## 3-brief follow-up batch (v2/v3 + Google-photo fallback) — implemented, NOT yet committed

The three untriaged briefs above implemented. All verified vs live source first (clean match — the
"BEFORE" snippets, line numbers, and `UserProfile.photoUrl` signature all matched). `compileDebugKotlin`
BUILD SUCCESSFUL. Briefs moved to `docs/DONE/`. **Working tree only — not yet committed/bumped/pushed.**

| Brief | File(s) | Notes / deviations |
|-------|---------|--------------------|
| backup-import-zlib-fix-v2 | JournalBackup.kt (importFrom) | Phase-2 extraction switched from `ZipInputStream` (sequential, discovers truncation mid-entry → opaque ZLIB error) to `java.util.zip.ZipFile` (reads central directory from end of file on open → a truncated `source.zip` throws `ZipException` immediately). Wrapped the `ZipFile` block in `try/catch(ZipException)` that rethrows a user-friendly "incomplete download — open in Files app to force full download" message via `error(...)`. Download phase (Phase 1, `CountingInputStream`+`copyTo`) unchanged. **Extra:** removed now-unused `import java.util.zip.ZipInputStream` (ZipOutputStream/ZipEntry still used by exportTo). As-brief. No strings. |
| map-default-position-v3 | MapScreen.kt | Removed the ±30° geographic-median outlier-rejection block from the camera `LaunchedEffect` — it dropped all-but-one location for globe-spanning travellers (Argentina/Iceland/Japan/Germany → only Germany survived → zoom-to-single). `latlngs` now built directly from the Null-Island filter; `nullFiltered` intermediate eliminated. delay/retry + `cameraPositioned` unchanged. As-brief. No strings. |
| profile-google-photo-fallback | ProfileScreen.kt | Three-tier avatar: `profilePhotoUri ?: user.photoUrl` as the Coil model; initials only when both null (email/password users w/o custom photo). Added `error = rememberVectorPainter(Icons.Default.Person)` so an unreachable Google URL (offline/revoked) falls to a Person icon, not a crash. Added imports `Icons.Default.Person` + `rememberVectorPainter` (`Icons` already imported). Camera badge overlay unchanged. As-brief. No strings. |

## vc33 SHIPPED — bundled tablet-ui + 3 follow-ups, bumped, pushed, WIF dispatched

`master` HEAD = `a1b6fd2`, pushed (`2245c32..a1b6fd2`); verified `origin/master` == local HEAD
(`a1b6fd2`) BEFORE dispatching. Commits since vc32 (`2245c32`):
- `4d2f41d` feat(ui) tablet layout improvements (was already local-only from the prior session).
- `0188ade` fix — the 3 follow-up briefs (ZipFile extraction / map global fit / profile Google photo).
- `a1b6fd2` Bump vc33 + refresh `default.txt` What's new (tablet + backup/map/profile).

`default.txt` refreshed: tablet layouts / reliable Drive restore / map fits all locations across
continents / profile photo falls back to Google account picture / bug fixes.

WIF release.yml dispatched as a separate command after the push landed →
**run `28010450490`, dispatched 2026-06-23 09:42:06 local (+02:00)**.
https://github.com/mictroid/macaco/actions/runs/28010450490
Did not watch (minimal-push convention). Not device-verified.

**vc33 run `28010450490` confirmed SUCCESS** (6m10s; started 07:42:08 UTC = dispatch 09:42 local,
completed ~07:48 UTC 2026-06-23). In closed testing.

## 3-brief batch (backup robustness + Drive account switch) — implemented

Three Cowork briefs implemented. All signatures verified vs live source first (clean match — function
bodies, line numbers, and import state all matched). `assembleDebug` BUILD SUCCESSFUL. Briefs moved to
`docs/DONE/`.

| Brief | File(s) | Notes / deviations |
|-------|---------|--------------------|
| backup-compact-export-v2 | JournalBackup.kt (writeCompressed) | Replaced single-pass `decodeStream` with two-pass decode: Pass 1 `inJustDecodeBounds` reads dimensions, compute smallest power-of-2 `inSampleSize` to bring longest edge within new `MAX_COMPACT_DIMENSION=2048`, Pass 2 decodes at that sample size with `RGB_565`. Fixes silent OOM-drop of large camera photos in compact export (681 MB→35 MB symptom). Added a `private companion object` (none existed) holding the constant. As-brief. No strings. |
| backup-import-large-file-v2 | JournalBackup.kt (importFrom) + SettingsScreen.kt | (1) Each photo-entry extraction wrapped in its own `runCatching` so one corrupt entry is skipped instead of aborting the whole import; `backup.json` extraction left unwrapped (still fatal). (2) `DisposableEffect(backupBusy)` in SettingsScreen sets/clears `FLAG_KEEP_SCREEN_ON` during export/import. Added `import androidx.compose.runtime.DisposableEffect` (was missing, as the brief flagged). As-brief. No strings. |
| drive-account-switch | DrivePhotoSync.kt (onAccountChanged) | Deletes `cacheDir/drive_photos/*` files (was only clearing the in-memory map) and emits `NotConnected` instead of `Idle` when the new GMS account lacks Drive scope, so SettingsScreen shows the reconnect prompt. `JavaFile`/`isDriveConnected()` already in scope. As-brief. No strings. |

## vc34 — bumped, notes refreshed, pushed, WIF dispatched

- Bumped versionCode 33→34 (versionName still 1.5, per convention). Refreshed `default.txt` What's new
  (compact backup keeps large photos / import skips damaged photo / screen stays awake / Drive cache
  clears on account switch).
- Commit `a90eff8` (batch + bump + notes). Pushed `500cdf1..a90eff8`; verified `origin/master` ==
  local HEAD (`a90eff8`) BEFORE dispatching.
- WIF release.yml dispatched as a separate command after the push landed →
  **run `28040379065`, dispatched 2026-06-23 18:21:48 local (+02:00)**.
  https://github.com/mictroid/macaco/actions/runs/28040379065
  Did not watch (minimal-push convention); confirm later with `gh run list`.

## profile-footer-height brief — commit `5f6af26` (local-only, NOT bumped/pushed/published)

`code-brief-profile-footer-height.md` implemented. Verified vs live source first (clean match).
ProfileScreen footer `Box` height 160dp→80dp + logo `Image` size 120dp→64dp so the scrollable
stats area fits without scrolling on typical phones/tablets; scrim overlay unchanged. No imports,
no strings, single file. `assembleDebug` BUILD SUCCESSFUL. Brief → `docs/DONE/`. **Rides with the
next batch** (vc34 was already dispatched when this arrived; user chose commit-only).

## map-v4 + tablet-ui-v2 briefs — commit `dcc7d2d` (local-only, NOT bumped/pushed/published)

Two briefs implemented, verified vs live source first. `assembleDebug` BUILD SUCCESSFUL. Briefs →
`docs/DONE/`. **Ride with the next batch alongside `5f6af26`.**

| Brief | File | Notes / deviations |
|-------|------|--------------------|
| map-default-position-v4 | MapScreen.kt | Moved `buildUpdate()` (incl. `newLatLngBounds`) inside the `try` after `delay(100)` so it can't throw before the map lays out — the early throw escaped the LaunchedEffect silently, left `cameraPositioned=false`, and stranded the scrim until the 8s timeout (map then showed default position). Also filter `latlngs` from current `locations` (entry strings looked up in `geocodedLocations`) instead of `geocodedLocations.values` (append-only cache w/ stale deleted/edited entries). **Deviation:** added `import com.google.android.gms.maps.CameraUpdate` — the brief claimed it was already imported but only `CameraUpdateFactory` was. No strings. |
| tablet-ui-v2 | NewEditEntryScreen.kt | Moved `horizontal = formHorizontalPadding` from LazyColumn `contentPadding` to `Modifier.padding(horizontal=...)` so `fillMaxWidth()` fields are actually constrained on sw600dp+ tablets (contentPadding clips but doesn't reduce measured width). Vertical contentPadding (12.dp) kept. `padding` import already present. As-brief. No strings. |

## compact-export-v3 + backup-loading-screen briefs — commit `9f09c7c`

Two more briefs implemented, verified vs live source first. `assembleDebug` BUILD SUCCESSFUL. Briefs
→ `docs/DONE/`.

| Brief | File(s) | Notes / deviations |
|-------|---------|--------------------|
| backup-compact-export-v3 | JournalBackup.kt (writeCompressed) | Encode JPEG into a `ByteArrayOutputStream` then `baos.writeTo(out)` in one call — keeps the encoder's chunked writes off the `ZipOutputStream` DEFLATE state (prevented `IOException` mid-entry). Replaced always-true `.isSuccess` with `getOrDefault(false)` + `baos.size()==0` guard so silently-failed encodes (compress→false) no longer produce empty zip entries. As-brief. No strings. |
| backup-loading-screen | SettingsScreen.kt + strings ×11 | Full-screen branded overlay (logo + spinner + contextual status text) shown while `backupBusy`; wrapped the `Scaffold` in a `Box(fillMaxSize)`, overlay as last child. Added `backupIsImport` flag set at all 3 launch sites (import/compact-export/full-export) + cleared at all 4 `backupBusy=false` sites. 4 new strings ×11 locales. **Deviation:** `JournalBackup.ImportPhase` referenced fully-qualified (`com.houseofmmminq.macaco.data.sync.JournalBackup.ImportPhase`) — the brief claimed it was imported but it isn't; matched the existing progress card's fully-qualified usage. |

## vc35 SHIPPED — bundled 5-brief batch, bumped, pushed, WIF dispatched

`master` HEAD = `26b0f03`, pushed (`3394f9a..26b0f03`); verified `origin/master` == local HEAD BEFORE
dispatching. Commits since vc34 (`3394f9a`):
- `5f6af26` profile-footer-height · `dcc7d2d` map-v4 + tablet-ui-v2 · `9f09c7c` compact-export-v3 +
  backup-loading-overlay · `26b0f03` bump vc35 + refreshed `default.txt` (+ docs commits `2cf47da`,
  `dd854e6`).
- `default.txt` refreshed: compact backups keep all photos / full-screen backup progress / map fits
  all locations / tablet form+profile polish / bug fixes.

WIF release.yml dispatched as a separate command after the push landed →
**run `28044169738`, dispatched 2026-06-23 19:24:25 local (+02:00)**.
https://github.com/mictroid/macaco/actions/runs/28044169738
Did not watch (minimal-push convention); confirm later with `gh run list`.

## Tooling (no app code) — skills + Cowork done-contract automation

After the vc35 ship, this session added developer tooling (commits `96ac988` → `90d6d1d`):
- **3 project skills** under `.claude/skills/`: `implement-cowork-brief`, `ship-to-play`,
  `bulk-localize-strings` (with a BOM-safe `insert-strings.template.ps1`).
- **Corrected the Cowork framing**: it drafts from the CORRECT repo since 2026-06-21 (not a stale
  clone); verify-first still applies because it makes import/signature mistakes (3 false "already
  imported" claims this session: DisposableEffect, CameraUpdate, JournalBackup.ImportPhase).
- **Cowork done-contract** documented + enforced: Cowork reads status from the filesystem + git
  history (docs/ root=pending, docs/DONE/=done, worklog=vc/commit/deviations). New `Stop` hook
  `.claude/hooks/check-brief-done-contract.sh` blocks finishing if a brief reached docs/DONE/ in
  unpushed/uncommitted work without a worklog entry.
- **Moved all brief hooks to tracked `.claude/settings.json`** (from gitignored settings.local.json)
  so a fresh clone gets them automatically.
- The root `macaco-brief.skill` / `macaco-status.skill` files are **Cowork's own** — left untouched.

## State / open
- **vc35 run `28044169738` CONFIRMED SUCCESS** (6m16s) — published to closed testing. 5-brief batch
  (profile footer / map v4 / tablet form v2 / compact-export v3 / backup loading overlay).
- **vc34 run `28040379065` CONFIRMED SUCCESS** (5m29s) — published. Backup robustness + Drive
  account-switch batch.
- Repo HEAD = `90d6d1d`, in sync with origin/master.
- **1 pending brief:** `docs/code-brief-forgot-password.md` (loose, not yet verified/implemented) →
  next session, joins the vc36 batch.
- vc33 PUBLISHED (run `28010450490` SUCCESS); vc32 PUBLISHED (run `28004897407` SUCCESS).
- Not device-verified: tablet-ui (no tablet here — confirm on Tab A9+) + the backup/Drive fixes.
- `.idea/deploymentTargetSelector.xml` left unstaged (perennial IDE noise).
- No loose briefs in `docs/` — all moved to `docs/DONE/`.
