What's new:
• Larger photos are now kept in compact backups instead of being dropped
• Backup import skips a damaged photo instead of failing the whole restore
• The screen stays awake during long backup imports
• Drive photo cache clears correctly when you switch Google accounts
• Bug fixes and polish
